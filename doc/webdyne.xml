<?xml version="1.0" encoding="UTF-8"?>
<!-- Converted by db4-upgrade version 1.0 -->
<article version="5.0" xmlns="http://docbook.org/ns/docbook">
  <info>
    <title>WebDyne Installation and Usage Guide</title>

    <author>
      <personname><firstname>Andrew</firstname><surname>Speer</surname></personname>

      <affiliation>
        <address>andrew.speer@isolutions.com.au</address>
      </affiliation>
    </author>

    <copyright>
      <year>2026</year>

      <holder role="mailto:andrew.speer@isolutions.com.au">Andrew Speer</holder>
    </copyright>
  </info>

  <section>
    <info>
      <title>Introduction</title>
    </info>

    <para>WebDyne is a Perl based dynamic HTML engine. It works with web servers (or from the command line) to render HTML documents with embedded Perl code.</para>

    <para>Once WebDyne is installed and initialised any file with a <filename>.psp</filename> extension (designated a PSP file) is treated as a WebDyne source file. It is parsed for WebDyne
    pseudo-tags (such as <markup>&lt;perl&gt;</markup> and <markup>&lt;block&gt;</markup>) which are interpreted and executed on the server. The resulting output is then sent to the
    browser.</para>

    <para>WebDyne works with common web server persistent Perl interpreters - such as Apache <command>mod_perl, </command><command>PSGI</command> and <command>PAGI</command> - to provide fast
    dynamic content. It works with PSGI servers such as Plack and <productname>Starman</productname>, and can be implemented as a Docker container to run HTML with embedded Perl code.</para>

    <para>Pages are parsed once, then stored in a partially compiled format - speeding up subsequent processing by avoiding the need to re-parse a page each time it is loaded.</para>

    <para>Alternate syntaxes are available to enable WebDyne code to be used with editors that do not recognise custom HTML tags, and the syntax supports the use of PHP type processing
    instruction tags (<markup>&lt;?..?&gt;</markup>) or <markup>&lt;div&gt;</markup> tags (via data attributes such as <markup>&lt;div data-webdyne-perl&gt;</markup>) to define WebDyne
    blocks.</para>

    <para>Perl code can be co-mingled in the HTML code for "quick and dirty" pages or completed isolated into separate files or modules for separation of presentation and logic layers. You can
    see examples in a dedicated section - but here are a few very simple examples as an overview.</para>

    <para>Simple HTML file with Perl code embedded using WebDyne :</para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction1.psp" xlink:show="new" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para>This can be abbreviated with some WebDyne shortcut tags such as <markup>&lt;start_html&gt;</markup>. This does exactly the same thing and still renders compliant HTML to the
    browser:</para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para>Don't like the co-mingling code and HTML but still want things in one file ?</para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para>Want further code and HTML separation ? You can import methods from any external Perl module. Example from a core module below, but could be any installed CPAN module or your own
    code:</para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para>Same concepts implemented in slightly different ways:</para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction5.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction5.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction6.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction6.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para>Using an editor that doesn't like custom tags ? Use of the <tag>&lt;div&gt;</tag> tag with a <literal>data-*</literal> attribute is valid HTML syntax and can be used to embed
    Perl:</para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction7.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction7.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para>Alternatively, put the code in a <tag>&lt;script&gt;</tag> block - it will be interpreted on the server, not the client:</para>

    <para><example>
        <info>
          <title>Basic Example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/introduction8.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/introduction7.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example></para>

    <para>Template blocks and variable replacement are supported also:</para>

    <example>
      <info>
        <title>Template Example</title>
      </info>

      <para><programlisting language="html"><xi:include href="example/introduction9.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
      xlink:href="https://demo.webdyne.org/example/introduction9.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
    </example>
  </section>

  <section xml:id="installation_quickstart">
    <info>
      <title>Installation and Quickstart</title>
    </info>

    <para>WebDyne will install and run on any modern Linux system that has a recent version of Perl installed and is capable of installing Perl modules via CPAN. Installation via Docker is
    also supported.</para>

    <para>When installing WebDyne there are two components which are required before you can begin serving PSP files:</para>

    <itemizedlist>
      <listitem>
        <para>The core WebDyne Perl modules</para>
      </listitem>

      <listitem>
        <para>A web server or application configured to use WebDyne.</para>
      </listitem>
    </itemizedlist>

    <para>WebDyne will work with Apache mod_perl, PAGI or PSGI compatible web servers (such as Plack, Starman etc.).</para>

    <para>Docker containers with pre-built versions of WebDyne are also available.</para>

    <section xml:id="quickstart">
      <title>Quickstart</title>

      <para>If using PSGI you can start a quick web server by creating a simple app.psp file:</para>

      <programlisting>#  Save this as app.psp
#
&lt;start_html&gt;
My first WebDyne page. Server time: &lt;? localtime ?&gt;</programlisting>

      <para>Then test install:</para>

      <para><screen language="bash">#  Install WebDyne
#
$ <command>cpanm WebDyne</command>

#  Render the file to STDOUT to see the HTML and check basic installation
#
$ <command>wdrender app.psp</command>

#  Install Plack if not already done.
#
$ <command>cpanm Plack</command>

#  Check all working
#
$ <command>webdyne.psgi --test</command>

#  Start serving only a single PSP file
#
$ <command>webdyne.psgi app.psp</command>

# Start serving files from the current directory. Directory requests use WebDyne's
# built-in index page by default.
#
$ <command>webdyne.psgi .</command>

# Start but listen on non-default port, only on localhost
#
$ <command>webdyne.psgi --port=5001 --host=127.0.0.1</command></screen>Connect your browser to the host and you should see the WebDyne output.</para>

      <para>You can shortcut install of Plack/Starman versions via:</para>

      <screen>#  Install WebDyne Plack (PSGI)
#
$ <command>cpanm Task::WebDyne::Plack</command>


#  Or Starman. Note if Starman install fails you may need to force install of Net::Server via
#  cpanm --force Net::Server
#  and try again
#
$ <command>cpanm Task::WebDyne::Starman</command></screen>
    </section>


    <section xml:id="cpan_install">
      <title>CPAN or CPANMinus Install</title>

      <para>Install from the Perl CPAN library using <command>cpan</command> or <command>cpanm</command> utilities. Installs dependencies if required (also from CPAN).</para>

      <para>Destination of the installed files is dependent on the local CPAN configuration, however in most cases it will be to the Perl site library location. WebDyne supports installation
      to an alternate location using the PREFIX option in CPAN. Binaries are usually installed to <filename>/usr/bin</filename> or <filename>/usr/local/bin</filename>, but may vary by
      distribution/local configuration.</para>

      <para>Assuming your CPAN environment is setup correctly you can run the command:</para>

      <para><command>perl -MCPAN -e "install WebDyne"</command></para>

      <para>Or (with <command>cpanminus</command> if installed)</para>

      <para><command>cpanm WebDyne</command></para>

      <para>This will install the base WebDyne modules, which includes the Apache config utility and PSGI and PAGI versions. Note that Apache or PSGI/PAGI servers and dependencies (such as
      Plack or Starman) are <emphasis role="bold">not</emphasis> installed by default and need to be installed separately - see the relevant section, or just run:</para>

      <screen><command>cpanm Task::WebDyne::Plack</command></screen>

      <para>to get everything needed to run with Plack/PSGI. For PAGI run:</para>

      <screen><command>cpanm Task::WebDyne::PAGI</command></screen>

      <para>If using Apache you will need to configure your system to use WebDyne to serve files with the <filename>.psp</filename> extension - see Apache install section.</para>
    </section>

    <section xml:id="runtime_choices">
      <title>Runtime Choices</title>

      <section xml:id="psgi">
        <title>PSGI</title>

        <para>Ensure that <productname>Plack</productname> is installed on your system via CPAN after installing WebDyne:</para>

        <screen># Via CPAN
#
<command>perl -MCPAN -e 'install Plack'</command>

# Modern systems
#
<command>cpan Plack</command>

# Or better via CPANM
#
<command>cpanm Plack</command>

# Or just do the whole lot in one hit. For WebDyne + Plack
#
<command>cpanm Task::WebDyne::Plack
</command>
# Or WebDyne + Plack + Starman. Note if Starman install fails you may need to force install of
# Net::Server via :
# cpanm --force Net::Server
#
<command>cpanm Task::WebDyne::Starman</command></screen>

        <para>you can then start a basic WebDyne server by running the webdyne.psgi command with the --test parameter</para>

        <para><screen><command>webdyne.psgi --test</command></screen></para>

        <para>This will start a PSGI web server on your machine listening to port 5000 (or port 5001 on a Mac). Open a connection to http://127.0.0.1:5000/ or the IP address of your server in
        your web browser to view the test page and validate the WebDyne is working correctly:</para>

        <para><screenshot>
            <mediaobject>
              <imageobject>
                <imagedata fileref="images/webdyne_psgi1.png" scale="30"/>
              </imageobject>
            </mediaobject>
          </screenshot></para>

        <para>Once verified as working correctly you can serve WebDyne content from a particular directory - or from a single file - using the syntax:</para>

        <screen>#  To serve up all files in a directory. Directory requests use WebDyne's built-in
#  index page by default.
#
<command>$ webdyne.psgi &lt;directory&gt;</command>

#  E.g serve files in /var/www/html. By default WebDyne will display its built-in
#  index page if no filename is specified.
#
<command>$ webdyne.psgi /var/www/html</command>

#  Or just a single app.psp file. Only this file will be served regardless of URL
#
<command>$ webdyne.psgi /var/www/html/time.psp</command></screen>

        <tip>
          <para>Starting WebDyne this way enables wrapper-managed index handling and full error messages with code backtraces if any errors are encountered. Use <option>--index=FILE</option>
          or <envar>DOCUMENT_DEFAULT</envar> to nominate a site-local default page, or <option>--no-index</option> to rely on the underlying request layer default document behavior.</para>
        </tip>

        <para>To start with <command>plackup</command>:</para>

        <screen>#  Start WebDyne via plackup in the current directory. A file named app.psp must exist,
#  directory indexing will not be performed.
#
<command>DOCUMENT_ROOT=. plackup `which webdyne.psgi`
</command>
#  Start serving a single file
#
<command>DOCUMENT_ROOT=./time.psp plackup /opt/perl5/bin/webdyne.psgi
</command>
#  Start with some Plack middleware added
#
<command>DOCUMENT_ROOT=./time.psp  plackup -e 'enable Plack::Middleware::Debug' `which webdyne.psgi`</command></screen>

        <para>The above starts a single-threaded web server using Plack. To start the more performant Starman server (assuming installed):</para>

        <screen>#  Start Starman instance. Substitute port + document root and location of webdyne.psgi
#  as appropriate for your system.
#
$ <command>DOCUMENT_ROOT=/var/www/html starman --port 5001 /usr/local/bin/webdyne.psgi</command></screen>

        <note>
          <para>Plack (via <command>webdyne.psgi</command> or <command>plackup</command>) and Starman versions of WebDyne will serve basic static files such as css, js, jpg etc. If you want
          more control over non PSP files you should use a traditional web server front end to serve those assets. Also note the Starman and plackup instances of WebDyne do not support the
          --test option or indexing - it assumes you are running in a production environment and have checked everything with the Plack implementation of <command>webdyne.psgi</command>
          first.</para>
        </note>

        <para>Numerous options can be set from the command line via environment variables, including WebDyne configuration. See relevant section for all WebDyne configuration options but
        assuming a local file <filename>webdyne.conf.pl</filename>:</para>

        <screen>#  Start instance webdyne.psgi using local config file
#
$ <command>WEBDYNE_CONF=./webdyne.conf.pl webdyne.psgi --port=5012 .</command></screen>

        <para>WebDyne can be incorporated into a traditional app.psgi startup file for PSGI in the following form. In this example any file with a .psp extension is routed to WebDyne for
        rendering<example>
            <info>
              <title>PSGI Application Startup</title>
            </info>

            <para><programlisting language="perl">#  Save as app.psgi
#
use strict;
use warnings;
use Plack::Builder;
use WebDyne::PSGI;

my $home_app = sub {
    return [
        200,
        [ 'Content-Type' =&gt; 'text/plain' ],
        [ "Welcome to the Home Page\n" ]
    ];
};

my $webdyne_app = WebDyne::PSGI-&gt;new(
    root  =&gt; '.',
    index =&gt; 1,
)-&gt;to_app;

builder {
    sub {
        my $env = shift;
        # Serve any URL ending in .psp through WebDyne
        if ($env-&gt;{PATH_INFO} =~ /\.psp$/i) {
            return $webdyne_app-&gt;($env);
        }

        return $home_app-&gt;($env);
    };
};
</programlisting>Save as app.psgi and start with command line:<screen>#  Assuming app.psgi in current directory
#
plackup


#  Or if named differently
#
plackup myapp.psgi</screen></para>
          </example></para>

        <para>Or you can mount all files from a particular directory against the WebDyne render engine.<example>
            <info>
              <title>PSGI Mount Startup</title>
            </info>

            <para><programlisting language="perl">use strict;
use warnings;
use Plack::Builder;
use WebDyne::PSGI;

# Main app for /
my $home_app = sub {
    return [
        200,
        [ 'Content-Type' =&gt; 'text/plain' ],
        [ "Welcome to the Home Page\n" ]
    ];
};

# Mount everything using builder
builder {
    mount '/'         =&gt; $home_app;
    mount '/webdyne'  =&gt; WebDyne::PSGI-&gt;new( root =&gt; '.')-&gt;to_app;
};</programlisting></para>
          </example></para>
      </section>

      <section xml:id="pagi">
        <title>PAGI</title>

        <para>WebDyne can also run as a <productname>PAGI</productname> application. PAGI support covers normal HTTP requests and, when using a PAGI server with the relevant scope support,
        server-sent events, WebSocket connections and application lifespan events.</para>

        <para>Ensure that <productname>PAGI</productname> is installed on your system via CPAN after installing WebDyne:</para>

        <screen># Via CPAN
#
<command>perl -MCPAN -e 'install PAGI'</command>

# Modern systems
#
<command>cpan PAGI</command>

# Or better via CPANM
#
<command>cpanm PAGI</command>

# Or just do the whole lot in one hit. For WebDyne + PAGI
#
<command>cpanm Task::WebDyne::PAGI
</command></screen>

        <para>you can then start a basic WebDyne server by running the webdyne.pagi command with the --test parameter</para>

        <para><screen><command>webdyne.pagi --test</command></screen></para>

        <para>This will start a PAGI web server on your machine listening to port 5000 (or port 5001 on a Mac). When started through <command>webdyne.pagi</command> the server binds to
        <literal>0.0.0.0</literal> unless a host is specified on the command line. Open a connection to http://127.0.0.1:5000/ (or the IP address of your server) in your web browser to view
        the test page and validate the WebDyne is working correctly:</para>

        <para><screenshot>
            <mediaobject>
              <imageobject>
                <imagedata fileref="images/webdyne_psgi1.png" scale="30"/>
              </imageobject>
            </mediaobject>
          </screenshot></para>

        <para>Once verified as working correctly you can serve WebDyne content from a particular directory - or from a single file - using the syntax:</para>

        <screen>#  To serve up all files in a directory. Directory requests use WebDyne's built-in
#  index page by default.
#
<command>$ webdyne.pagi &lt;directory&gt;</command>

#  E.g serve files in /var/www/html. By default WebDyne will display its built-in
#  index page if no filename is specified.
#
<command>$ webdyne.pagi /var/www/html</command>

#  Or just a single app.psp file. Only this file will be served regardless of URL
#
<command>$ webdyne.pagi /var/www/html/time.psp</command></screen>

        <tip>
          <para>Starting WebDyne this way enables wrapper-managed index handling and full error messages with code backtraces if any errors are encountered. Use <option>--index=FILE</option>
          or <envar>DOCUMENT_DEFAULT</envar> to nominate a site-local default page, or <option>--no-index</option> to rely on the underlying request layer default document behavior.</para>
        </tip>

        <para>To start with <command>pagi-server</command>:</para>

        <screen>#  Start WebDyne via pagi-server in the current directory. A file named app.psp must exist,
#  directory indexing will not be performed.
#
<command>DOCUMENT_ROOT=. pagi-server --app `which webdyne.pagi` --port=5000 --host=0.0.0.0
</command>
#  Start serving a single file
#
<command>DOCUMENT_ROOT=./time.psp pagi-server /opt/perl5/bin/webdyne.pagi
</command>
</screen>

        <para>The above starts a single-threaded web server using PAGI.</para>

        <note>
          <para>PAGI (via <command>webdyne.pagi</command> or <command>pagi-server</command>) versions of WebDyne will serve basic static files such as css, js, jpg etc. If you want more
          control over non PSP files you should use a traditional web server front end to serve those assets.</para>
        </note>

        <para>Numerous options can be set from the command line via environment variables, including WebDyne configuration. See relevant section for all WebDyne configuration options but
        assuming a local file <filename>webdyne.conf.pl</filename>:</para>

        <screen>#  Start instance webdyne.pagi using local config file
#
$ <command>WEBDYNE_CONF=./webdyne.conf.pl webdyne.pagi --port=5012 .</command></screen>

        <para>WebDyne can be incorporated into a traditional app.pagi/app.pl startup file for PAGI in the following form. In this example any file with a .psp extension is routed to WebDyne
        for rendering:<example>
            <info>
              <title>PAGI Application Startup</title>
            </info>

            <para><programlisting language="perl">use strict;
use warnings;
use Future::AsyncAwait;
use experimental 'signatures';

use PAGI::Middleware::Builder;
use WebDyne::PAGI;

my $home_app = async sub ($scope, $receive, $send) {
    die "Unsupported scope type: $scope-&gt;{type}"
        unless $scope-&gt;{type} eq 'http';

    await $send-&gt;({
        type    =&gt; 'http.response.start',
        status  =&gt; 200,
        headers =&gt; [
            [ 'content-type', 'text/plain' ],
        ],
    });

    await $send-&gt;({
        type =&gt; 'http.response.body',
        body =&gt; "Welcome to the Home Page\n",
        more =&gt; 0,
    });
};

my $webdyne_app = WebDyne::PAGI-&gt;new(
    root  =&gt; '.',
)-&gt;to_app;

builder {
    async sub ($scope, $receive, $send) {
        if (($scope-&gt;{path} // '') =~ /\.psp$/i) {
            return await $webdyne_app-&gt;($scope, $receive, $send);
        }

        return await $home_app-&gt;($scope, $receive, $send);
    };
};
</programlisting></para>
          </example></para>
      </section>

      <section xml:id="apache_mod_perl">
        <title>Apache mod_perl</title>

        <para>If using Apache with mod_perl you can initialise WebDyne using the <command>wdapacheinit</command> command. This will attempt to auto-discover where the Apache binary and
        configuration files are, then add a suitable <filename>webdyne.conf</filename> file to the apache configuration. Apache will need to be restarted for the new configuration file to take
        effect. This will need to be done as a the root user.</para>

        <para><screen language="bash">[root@localhost ~]# <command>wdapacheinit</command>

[install] - Installation source directory '/usr'.
[install] - Creating cache directory '/var/cache/webdyne'.

[install] - Writing Apache config file '/etc/httpd/conf.d/webdyne.conf'.
[install] - Writing Webdyne config file '/etc/httpd/conf.d/webdyne_conf.pl'.
[install] - Apache uses conf.d directory - not changing httpd.conf file.
[install] - Granting Apache (apache.apache) ownership of cache directory '/var/cache/webdyne'.
[install] - Install completed.

[root@localhost ~]# <command>systemctl restart httpd</command></screen></para>

        <para>By default WebDyne will create a cache directory in <filename>/var/cache/webdyne</filename> on Linux systems when a default CPAN install is done (no PREFIX specified). If a
        PREFIX is specified the cache directory will be created as <filename>$PREFIX/cache</filename>. Use the <command>wdapacheinit</command> <option>--cache</option> command-line option to
        specify an alternate location.</para>

        <para>Once <command>wdapacheinit</command> has been run the Apache server should be reloaded or restarted. Use a method appropriate for your Linux distribution.</para>

        <screen>
[root@localhost ~]# <command>systemctl httpd restart</command>
Stopping httpd:                                            [  OK  ]
Starting httpd:                                            [  OK  ]
</screen>

        <section>
          <info>
            <title>Manual Apache Configuration</title>
          </info>

          <para>If the <command>wdapacheinit</command> command does not work as expected on your system then the Apache configuration files can be modified manually.</para>

          <para>Include the following section in the Apache <filename>httpd.conf</filename> file (or create a <filename>webdyne.conf</filename> file if you distribution supports
          <filename>conf.d</filename> style configuration files). The following configuration files are written with Apache 2.4 syntax - adjust path and syntax as required:</para>

          <para><screen>#  Need mod_perl, load up if not already done. Adjust path according to your distro.
#
&lt;IfModule !mod_perl.c&gt;
LoadModule perl_module "/etc/httpd/modules/mod_perl.so"
&lt;/IfModule&gt;

#  Uncomment and update if using a local::lib location for Perl modules
#
#PerlSwitches -I/opt/perl -I/opt/otherperl

#  Preload the WebDyne and WebDyne::Compile module
#
PerlModule    WebDyne WebDyne::Compile

#  Associate psp files with WebDyne
#
AddHandler    modperl    .psp
PerlHandler   WebDyne

#  Set a directory for storage of cache files. Make sure this exists already is writable by the
#  Apache daemon process.
#
PerlSetVar    WEBDYNE_CACHE_DN    '/opt/webdyne/cache'

#  Allow Apache to access the cache directory if it needs to serve pre-compiled pages from there.
#
&lt;Directory "/opt/webdyne/cache"&gt;
Require all granted
&lt;/Directory&gt;

# Put variables in a separate file - best
#
PerlRequire conf.d/webdyne_constant.pl

#  Or use &lt;Perl&gt; sections - but warning, certbot doesn't like this syntax in http conf files
#
&lt;Perl&gt;

#  Error display/extended display on/off. Set to 1 to enable, 0 to disable
#
$WebDyne::WEBDYNE_ERROR_SHOW=1;
$WebDyne::WEBDYNE_ERROR_SHOW_EXTENDED=1;
&lt;/Perl&gt;

</screen></para>

          <important>
            <para>Substitute directory paths in the above example for the relevant/correct/appropriate ones on your system.</para>
          </important>

          <para>Create the cache directory and assign ownership and permission appropriate for your distribution (group name will vary by distribution - locate the correct one for your
          distribution)</para>

          <screen>
[root@localhost ~]# <command>mkdir /opt/webdyne/cache
</command>[root@localhost ~]# <command>chgrp apache /opt/webdyne/cache
</command>[root@localhost ~]# <command>chmod 770 /opt/webdyne/cache
</command></screen>

          <para>Restart Apache and check for any errors.</para>
        </section>

        <section>
          <info>
            <title>Development Apache Runner</title>
          </info>

          <para>A convenience utility <command>webdyne.apache</command> is supplied which starts a single-threaded instance of Apache on port 5000 to render WebDyne pages. It works in the same
          way as the PSGI and PAGI startup scripts outlined later</para>

          <para><screen>#  Start an Apache test instance on a single .psp file
#
$ perl -Ilib bin/webdyne.apache ./time.psp
/usr/sbin/httpd -D ONE_PROCESS -d /tmp/webdyne_apache_ol2EWnkb -f /tmp/webdyne_apache_ol2EWnkb/conf/httpd.conf -D APACHE2 -D APACHE2_4 -D PERL_USEITHREADS
using Apache/2.4.62 (event MPM)

waiting 60 seconds for server to start: .
[Sat May 02 16:56:20.700397 2026] [core:trace3] [pid 356673:tid 356673] core.c(3505): Setting LogLevel for all modules to trace8
[Sat May 02 16:56:21.115354 2026] [mpm_event:debug] [pid 356673:tid 356717] event.c(2402): AH02471: start_threads: Using epoll (wakeable)

waiting 60 seconds for server to start: ok (waited 0 secs)
server localhost:5000 started


#  Start an instance to with a simple directory index on the current directory
#
$ perl -Ilib bin/webdyne.apache .


</screen></para>
        </section>
      </section>
    </section>

    <section xml:id="docker">
      <title>Docker</title>

      <para>Docker containers are available from the <link xlink:href="https://github.com/aspeer/WebDyne/pkgs/container/webdyne" xmlns:xlink="http://www.w3.org/1999/xlink">Github Container
      Registry</link>. Install the default Docker container (based on Debian) via:</para>

      <screen>#  Default debian version
#
$ <command>docker pull ghcr.io/aspeer/webdyne:latest</command>

#  Or Alpine/Fedora/Perl versions
#
# docker pull ghcr.io/aspeer/webdyne:alpine
# docker pull ghcr.io/aspeer/webdyne:fedora
</screen>

      <para>Start the docker container with the command:</para>

      <screen>$ <command>docker run -e PORT=5002 -p 5002:5002 --name=webdyne ghcr.io/aspeer/webdyne:latest</command></screen>

      <para>This will start WebDyne running on port 5002 on the host. Connecting to that location should show the server <emphasis>localtime</emphasis> test page</para>

      <para>By default the container starts WebDyne with <command>starman</command> using the PSGI runner. The server implementation can be selected with the <envar>WEBDYNE_SERVER</envar>
      environment variable.</para>

      <screen>#  Run with the default PSGI/Starman server
#
$ <command>docker run -e PORT=5002 -p 5002:5002 --name=webdyne ghcr.io/aspeer/webdyne:latest</command>

#  Run with the PAGI server
#
$ <command>docker run -e WEBDYNE_SERVER=pagi -e PORT=5002 -p 5002:5002 --name=webdyne ghcr.io/aspeer/webdyne:latest</command></screen>

      <para>To mount a local page and serve it through the docker container use the command:</para>

      <screen>$ <command>docker run -v $(pwd):/app:ro -e PORT=5011 -e DOCUMENT_ROOT=/app -p 5011:5011 --name webdyne ghcr.io/aspeer/webdyne:latest</command></screen>

      <para>This will tell docker to mount the local directory into the docker container. Because the container starts WebDyne through <command>starman</command> or
      <command>pagi-server</command>, the default document is <filename>app.psp</filename> unless <envar>DOCUMENT_DEFAULT</envar> is set. If there is a <filename>cpanfile</filename> in the
      mount directory any modules will be installed into the docker container automatically.</para>

      <para>When the container is started without an explicit command, the default entrypoint uses the following environment variables:</para>

      <variablelist>
        <varlistentry>
          <term><envar>PORT</envar></term>

          <listitem>
            <para>The TCP port the selected server listens on inside the container. If omitted, the entrypoint uses <literal>8080</literal>. The docker <option>-p</option> mapping should map the
            same container port, for example <option>-e PORT=5002 -p 5002:5002</option>.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><envar>WEBDYNE_SERVER</envar></term>

          <listitem>
            <para>Selects the server used by the default container command. Supported values are <literal>psgi</literal> and <literal>pagi</literal>. If omitted, <literal>psgi</literal> is used.
            <literal>psgi</literal> runs <command>starman -MWebDyne</command> with <command>webdyne.psgi</command>. <literal>pagi</literal> runs <command>pagi-server</command> with
            <command>webdyne.pagi</command>, enabling PAGI request handling such as SSE and WebSocket support where the application uses those features. Any other value causes the entrypoint to
            exit with an error.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><envar>DOCUMENT_ROOT</envar></term>

          <listitem>
            <para>The WebDyne document root passed through to <command>webdyne.psgi</command> or <command>webdyne.pagi</command>. The published images default this to <filename>/app</filename>.
            Override it when mounting application files somewhere else in the container.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><envar>DOCUMENT_DEFAULT</envar></term>

          <listitem>
            <para>The default document to serve from <envar>DOCUMENT_ROOT</envar>. If omitted, the command wrappers enable WebDyne's built-in default index behavior, which uses
            <filename>app.psp</filename> in the published Docker images.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><envar>WEBDYNE_CACHE_DN</envar></term>

          <listitem>
            <para>The WebDyne compile/cache directory. The published images set this to <filename>/opt/webdyne/cache</filename> by default via the image's
            <envar>PERL_CARTON_PATH</envar>. Override it if the cache should live on a mounted volume or another writable directory.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><envar>PERL_CARTON_PATH</envar></term>

          <listitem>
            <para>The local Perl installation prefix inside the image. The published images default this to <filename>/opt/webdyne</filename> and use it to set <envar>PERL5LIB</envar>,
            <envar>PATH</envar>, the default <envar>WEBDYNE_CACHE_DN</envar>, and the path to the bundled <command>webdyne.psgi</command> and <command>webdyne.pagi</command> wrappers. This is
            normally only changed when building a custom image.</para>
          </listitem>
        </varlistentry>
      </variablelist>

      <para>See the <link linkend="docker_runtime_server_tuning">Docker Runtime Server Tuning</link> reference section for server worker, timeout and connection limit environment variables.</para>

    </section>
  </section>

  <section xml:id="examples">
    <info>
      <title>Basic Usage</title>
    </info>

    <para>Assuming the installation has completed with no errors you are now ready to start creating WebDyne pages and applications.</para>

    <section>
      <info>
        <title>Integrating Perl into HTML</title>
      </info>

      <para>Some code fragments to give a very high-level overview of how WebDyne can be implemented. First the most basic usage example:</para>

      <para><example>
          <info>
            <title>Basic Example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/hello1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/>
</programlisting><link xlink:href="https://demo.webdyne.org/example/hello1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>So far not too exciting - after all we are mixing code and content. Lets try again - the &lt;perl&gt; tag can take a <property>handler</property> attribute which references (calls)
      a perl subroutine - in this case embedded in the PSP file under the <token>__PERL__</token> token.</para>

      <para><example>
          <info>
            <title>Basic Example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/hello2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/hello2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example><note>
          <para>In this case the &lt;perl&gt; tag is implicitly closed (contains no content) as it is assumed all content will be returned by the handler (i.e. the "hello" routine).</para>
        </note></para>

      <para>Better - at least code and content are distinctly separated. Note that whatever the Perl code returns at the end of the routine is what is displayed. Although WebDyne will happily
      display returned strings or scalars, it is more efficient to return a scalar reference, e.g.:</para>

      <para><programlisting language="perl">#  Works
#
sub greeting { print "Hello World" }


#  Is the same as
#
sub greeting { return "Hello World" }
sub greeting { my $var="Hello World"; return $var }


# But best is
#
sub greeting { my $var="Hello World"; return \$var }


# If you don't want to display anything return \undef,
#
sub greeting { return \undef }

</programlisting></para>

      <para>Up until now all the Perl code has been contained within the WebDyne file. The following example shows an instance where the code is contained in a separate Perl module, which
      should be available somewhere in the <filename>@INC</filename> path.</para>

      <para><example>
          <info>
            <title>Basic Example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/hello3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/hello3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>If not already resident the module (in this case <classname>Digest::MD5</classname>) will be loaded by WebDyne, so it must be available somewhere in the <filename>@INC</filename>
      path. Alternate syntaxes are supported - these all work in the same way:</para>

      <para><example>
          <info>
            <title>Basic Example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/hello3a.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/hello3a.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <section>
        <info>
          <title>Use of the &lt;perl&gt; tag for in-line code.</title>
        </info>

        <para>The above examples show several variations of the <tag>&lt;perl&gt;</tag> tag in use. Perl code that is enclosed by <tag>&lt;perl&gt;..&lt;/perl&gt;</tag> tags is designated as
        <emphasis>in-line</emphasis> code:</para>

        <para><example>
            <info>
              <title>Basic Example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/inline1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/inline1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>This is the most straight-forward use of Perl within a HTML document, but does not really make for easy reading - the Perl code and HTML are intermingled. It may be OK for quick
        scripts etc, but a page will quickly become hard to read if there is a lot of in-line Perl code interspersed between the HTML.</para>

        <para>in-line Perl can be useful if you want a "quick" computation, e.g. insertion of the current year. Note the ability to require external modules via the require attribute, and
        import functions via the import attribute</para>

        <para><example>
            <info>
              <title>Basic Example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/inline2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/inline2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>Which can be pretty handy, but looks a bit cumbersome - the tags interfere with the flow of the text, making it harder to read. For this reason in-line perl can also be flagged
        in a WebDyne page using the shortcuts <tag>!</tag><emphasis><tag>{! .. !}</tag></emphasis>, or by the use of processing instructions (<emphasis><tag>&lt;? .. ?&gt;</tag></emphasis>)
        e.g.:</para>

        <para><example>
            <info>
              <title>Basic Example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/inline3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/inline3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>The <emphasis><tag>!{! .. !}</tag></emphasis> denotation can also be used in tag attributes (processing instructions, and <tag>&lt;perl&gt;</tag> tags cannot):</para>

        <para><example>
            <info>
              <title>In-line code in tag attributes</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/inline4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/inline4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>
      </section>

      <section>
        <info>
          <title>Use of the &lt;perl&gt; tag for non-inline code.</title>
        </info>

        <para>Any code that is not co-mingled with the HTML of a document is <emphasis>non-inline</emphasis> code. It can be segmented from the content HTML using the <token>__PERL__</token>
        token, or by being kept in a completely different package and referenced as an external Perl subroutine call. An example of non-inline code:</para>

        <para><example>
            <info>
              <title>Perl code example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/hello2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/hello2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>Note that the <tag>&lt;perl&gt;</tag> tag in the above example is explicitly closed and does not contain any content. However non-inline code can enclose HTML or text within the
        tags:</para>

        <para><example>
            <info>
              <title>Perl code example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/noninline1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/noninline1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>But this is not very interesting so far - the "Hello World" text is not displayed when the example is run !</para>

        <para>In order for text or HTML within a non-inline perl block to be displayed, it must be "rendered" into the output stream by the WebDyne engine. This is done by calling the
        <methodname>render()</methodname> method. Let's try that again:</para>

        <para><example>
            <info>
              <title>Perl code example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/noninline2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/noninline2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>And again, this time showing how to render the text block multiple times. Note that an array reference is returned by the Perl routine - this is fine, and is interpreted as an
        array of HTML text, which is concatenated and send to the browser.</para>

        <para><example>
            <info>
              <title>Perl code example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/noninline3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/noninline3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>Same output using the <code>$self-&gt;print()</code> method:</para>

        <example>
          <info>
            <title>Perl code example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/noninline3a.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/noninline3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>

        <para>Note that there is a syntactic shortcut available if you have a single handler - just call the subroutine "<methodname>handler</methodname>" and it will be invoked by
        default:</para>

        <example>
          <info>
            <title>Perl code example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/noninline_handler1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/noninline_handler1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>
      </section>

      <section>
        <info>
          <title>Requiring external modules and importing methods</title>
        </info>

        <para>As noted above inline perl code can import external modules (including any Perl core modules, any module installed via CPAN and/or any module in the normal Perl @INC path or any
        filesystem file). Syntax is as follows:</para>

        <programlisting language="perl">#  Require single module and import single method
#
&lt;perl require="Digest::MD5" import="md5_hex"/&gt;

#  Require single module and import multiple methods
#
&lt;perl require="Digest::MD5" import="md5_hex md5_base64"/&gt;

#  Alternate syntax using array attribute - see later section
#
&lt;perl require="Digest::MD5" import="@{qw(md5_hex md5_base64)}"/&gt;

#  You can also require a file. No import is available in this usage
#
&lt;perl require="mymodule.pm"&gt;

</programlisting>

        <warning>
          <para>The import functionality is very basic - it only maps to the function in the module, it does not call the actual module import() facility. If the module you are importing does
          something sophisticated in its import() function (such as auto-creating methods) this will not work. Additionally nothing is imported by default, even if automatically exported by a
          module.</para>
        </warning>

        <para>A very basic demo of the above would look as follows:</para>

        <example>
          <info>
            <title>Perl code example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/require1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/require1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>

        <para>If your module usage and import requirements are more sophisticated they should be put in a <token>__PERL__</token> section as "normal" code. Anything used or imported will be
        available to any code within the page:</para>

        <example>
          <info>
            <title>Perl code example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/require2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/require2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>
      </section>

      <section>
        <info>
          <title>Alternate output methods from Perl handlers</title>
        </info>

        <para>When calling a perl handler from a PSP file at some stage you will want your code to deliver output to the browser. Various examples have been given throughout this document,
        here is a summary of various output options:</para>

        <example>
          <title>Variations on output options</title>

          <para><programlisting language="html"><xi:include href="example/output1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/output1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>
      </section>

      <section>
        <info>
          <title>Passing parameters to subroutines</title>
        </info>

        <para>The behaviour of a called <token>__PERL__</token> subroutine can be modified by passing parameters which it can act on:</para>

        <para><example>
            <info>
              <title>Perl code example with parameters</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/noninline7.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/noninline7.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>
      </section>

      <section>
        <info>
          <title>Parameter inheritance</title>
        </info>

        <para>In-line code can inherit parameters passed from a perl handler/method. In-line code gets two parameters supplied - the first (<varname>$_[0]</varname>) is the self reference
        (e.g. <varname>$self</varname>), the second (<varname>$_[1]</varname>) is any inherited parameters, passed as a hash reference. This can be useful for quick "in-line" formatting,
        e.g:</para>

        <para><example>
            <info>
              <title>Perl code example with parameter inheritance</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/inherit1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/inherit1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>
      </section>

      <section>
        <info>
          <title>Notes about <token>__PERL__</token> sections</title>
        </info>

        <para>Code in <token>__PERL__</token> sections has some particular properties.<token> __PERL__</token> code is only executed once. Subroutines defined in a<token> __PERL__</token>
        section can be called as many times as you want, but the code outside of subroutines is only executed the first time a page is loaded. No matter how many times it is run, in the
        following code <varname>$i</varname> will always be 1:</para>

        <para><example>
            <info>
              <title>Perl code variable scope</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/noninline4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/noninline4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>Lexical variables are not accessible outside of the <token>__PERL__</token> section due to the way perl's eval() function works. The following example will fail:</para>

        <para><example>
            <info>
              <title>Perl code variable scope</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/noninline5.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/noninline5.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>Package defined vars declared in a <token>__PERL__</token> section do work, with caveats:</para>

        <para><example>
            <info>
              <title>Perl code variable scope</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/noninline6.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/noninline6.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>See the Variables/Substitution section for clean ways to insert variable contents into the page.</para>
      </section>
    </section>

    <section>
      <info>
        <title>Variables</title>
      </info>

      <para>WebDyne starts to get more useful when variables are used to modify the content of a rendered text block. A simple example:</para>

      <para><example>
          <info>
            <title>Variable usage</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/var1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/var1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>Note the passing of the <parameter>time</parameter> value as a parameter to be substituted when the text is rendered.</para>

      <para>Combine this with multiple call to the <methodname>render()</methodname> routine to display dynamic data:</para>

      <para><example>
          <info>
            <title>Variable usage</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/var2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/var2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>Variables can also be used to modify tag attributes:</para>

      <para><example>
          <info>
            <title>Variable usage</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/var3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/var3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>Other variable types are available also, including:</para>

      <itemizedlist>
        <listitem>
          <para><varname>@{var,var,..}</varname> for arrays, e.g. <varname>@{'foo', 'bar'}</varname></para>
        </listitem>

        <listitem>
          <para><varname>%{key=&gt;value, key=&gt;value, ..}</varname> for hashes e.g.<varname> %{ a=&gt;1, b=&gt;2 }</varname></para>
        </listitem>

        <listitem>
          <para><varname>+{varname}</varname> for CGI form parameters, e.g. <varname>+{firstname}</varname></para>
        </listitem>

        <listitem>
          <para><varname>*{varname} </varname>for environment variables, e.g. <varname>*{HTTP_USER_AGENT}</varname></para>
        </listitem>

        <listitem>
          <para><varname>^{requestmethod}</varname> for Apache request (<varname>$r=Apache-&gt;request</varname>) object methods, e.g. <varname>^{protocol}</varname>. Only available for in
          Apache/mod_perl, and only useful for request methods that return a scalar value.</para>
        </listitem>
      </itemizedlist>

      <para>The following template uses techniques and tags discussed later, but should provide an example of potential variable usage:</para>

      <para><example>
          <info>
            <title>Variable usage</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/var4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/var4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>
    </section>

    <section>
      <info>
        <title>Shortcut Tags</title>
      </info>

      <para>Previous versions of WebDyne used Lincoln Stein's CGI.pm module to render tags, and supported CGI.pm shortcut tags such as &lt;start_html&gt;, &lt;popup_menu&gt; etc. Modern
      versions of WebDyne do not use CGI.pm in any modules, having ported tag generation to HTML::Tiny. Support for shortcut tags is preserved though - they provide a quick and easy way to
      generate simple web pages.</para>

      <section>
        <info>
          <title>Quick pages using shortcut <tag>&lt;start_html&gt;</tag>, <tag>&lt;end_html&gt;</tag> tags</title>
        </info>

        <para>For rapid development you can take advantage of the <tag>&lt;start_html&gt;</tag> and <tag>&lt;end_html&gt;</tag> tags. The following page generates compliant HTML (view the page
        source after loading it to see for yourself):</para>

        <para><example>
            <info>
              <title>Shortcut tags</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/cgi6.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/cgi6.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example><tip>
            <para>The <tag>&lt;end_html&gt;</tag> tag can be omitted if desired, the HTML parser will auto close any dangling tags at the end of the document, including adding any omitted
            <tag>&lt;/body&gt;</tag> or <tag>&lt;/html&gt;</tag> tags. So even if omitted from the PSP file WebDyne should still generate valid HTML to be served to the browser.</para>
          </tip></para>

        <para>The <tag>&lt;start_html&gt;</tag> tag generates all the <tag>&lt;html&gt;</tag>, <tag>&lt;head&gt;</tag>, <tag>&lt;title&gt;</tag> tags etc needed for a valid HTML page plus an
        opening body tag. Just enter the body content, then optionally finish with <tag>&lt;end_html&gt;</tag> to generate the closing <tag>&lt;body&gt;</tag> and <tag>&lt;html&gt;</tag> tags
        (optional because the page is automatically closed if these are omitted). See the tag reference section but here is an example using several of the properties available in the
        <tag>&lt;start_html&gt;</tag> tag including loading multiple scripts and stylesheets:</para>

        <example>
          <info>
            <title>Shortcut tags</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/start_html1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/start_html1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>

        <caution>
          <para>Make sure any attributes using the <parameter>@{..}</parameter> or <parameter>%{..}</parameter> convention are on one line - the parser may not interpret them correctly if
          spanning multiple lines.</para>
        </caution>

        <para>If using the <tag>&lt;start_html&gt;</tag> shortcut tag you can optionally insert default stylesheets and/or <tag>&lt;head&gt;</tag> sections from the Webdyne configuration file.
        E.g if in your <filename>webdyne.conf.pl</filename> file you have the following:</para>

        <screen>$_={
    'WebDyne::Constant' =&gt; {

        #  Inserted as &lt;meta&gt; into &lt;head&gt; section
        #
        WEBDYNE_META =&gt; {
            #  These all rendered as &lt;meta name="$key" content="$value"&gt;
            viewport =&gt; 'width=device-width, initial-scale=1.0',
            author =&gt; 'Bob Foobar',
            #  This one rendered as &lt;meta http-equiv="X-UA-Compatible" content="IE=edge"&gt;
            'http-equiv=X-UA-Compatible' =&gt; 'IE=edge',
            'http-equiv=refresh' =&gt; '5; url=https://www.example.com'
        }

        #  This is inserted inside the &lt;html&gt; starting tag, works for &lt;start_html&gt; or straight &lt;html&gt;
        #
        WEBDYNE_HTML_PARAM =&gt; {
            lang =&gt; 'de'
        }

        #  This is inserted before the &lt;/head&gt; closing tag, works for &lt;start_html&gt; or straight &lt;html&gt;
        #
        WEBDYNE_HEAD_INSERT =&gt; &lt;&lt; 'END'
&lt;link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.classless.min.css"&gt;
&lt;style&gt;
:root { --pico-font-size: 85% }
body { padding-top: 10px; padding-left: 10px;
&lt;/style&gt;
END

    }
}</screen>

        <para>Then any <filename>PSP</filename> file with a <tag>&lt;start_html&gt;</tag> tag will have the following content:</para>

        <screen>&lt;!DOCTYPE html&gt;&lt;html lang="de"&gt;
&lt;head&gt;
&lt;title&gt;Untitled Document&lt;/title&gt;
&lt;meta name="viewport" content="width=device-width, initial-scale=1.0"&gt;
&lt;meta name="author" content="Bob Foobar"&gt;
&lt;meta http-equiv="refresh" content="5; url=https://www.example.com"&gt;
&lt;meta http-equiv="X-UA-Compatible" content="IE=edge"&gt;
&lt;link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.classless.min.css"&gt;
&lt;style&gt;
    :root { --pico-font-size: 85% }
&lt;/style&gt;
&lt;/head&gt;
</screen>
      </section>

      <section>
        <title>HTML Forms using &lt;popup_menu&gt;,&lt;checkbox_group&gt; and other tags</title>

        <para>The CGI.pm module presented several shortcut tags for generating HTML forms. These tags have been recreated in WebDyne and act in a similar way.</para>

        <para>The manual page for CGI.pm contains the following synopsis example:</para>

        <example>
          <info>
            <title>CGI.pm Example</title>
          </info>

          <para><programlisting>use CGI qw/:standard/;
   print header,
         start_html('A Simple Example'),
         h1('A Simple Example'),
         start_form,
         "What's your name? ",textfield('name'),p,
         "What's the combination?", p,
         checkbox_group(-name=&gt;'words',
                        -values=&gt;['eenie','meenie','minie','moe'],
                        -defaults=&gt;['eenie','minie']), p,
         "What's your favorite color? ",
         popup_menu(-name=&gt;'color',
                    -values=&gt;['red','green','blue','chartreuse']),p,
         submit,
         end_form,
         hr;

    if (param()) {
        print "Your name is",em(param('name')),p,
              "The keywords are: ",em(join(", ",param('words'))),p,</programlisting></para>
        </example>

        <para>If the example was ported to a WebDyne compatible page it might look something like this:</para>

        <para><example>
            <info>
              <title>CGI Example</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/cgi1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/cgi1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <note>
          <para>When using the WebDyne form tags, state (previous form values) are preserved after the Submit button is presented. This makes building single page application simple as there
          is no need to implement logic to adjust options in a traditional HTML form to reflect the user's choice.</para>
        </note>
      </section>

      <section>
        <info>
          <title>More on HTML shortcut tags in forms</title>
        </info>

        <para>Tags such as <tag>&lt;popup_menu&gt;</tag> output traditional HTML form tags such as <tag>&lt;select&gt;&lt;option&gt;...&lt;/select&gt;</tag>, but they have the advantage of
        allowing Perl data types as attributes. Take the following example:</para>

        <screen>
&lt;popup_menu values="%{red=&gt;Red, green=&gt;Green, blue=&gt;Blue}"/&gt;

</screen>

        <para>it is arguably easier to read than:</para>

        <screen>
&lt;select name="values" tabindex="1"&gt;
&lt;option value="green"&gt;Green&lt;/option&gt;
&lt;option value="blue"&gt;Blue&lt;/option&gt;
&lt;option value="red"&gt;Red&lt;/option&gt;
&lt;/select&gt;

</screen>

        <para>So there is some readability benefit, however the real advantage shows when we consider the next example:<example>
            <info>
              <title>Dynamic tag attributes</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/cgi5.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/cgi5.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>All values for the menu item were pre-populated from one WebDyne variable - which saves a significant amount of time populating a "countries" style drop-down box.</para>
      </section>

      <section>
        <info>
          <title>Access to HTML form responses and query strings</title>
        </info>

        <para>Once a form is submitted you will want to act on responses. There are several ways to do this - you can access a CGI::Simple object instance in any WebDyne template by calling
        the CGI() method to obtain form responses:</para>

        <para><example>
            <info>
              <title>Access to CGI object</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/cgi3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/cgi3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>From there you can all any method supported by the CGI::Simple module - see the CGI::Simple manual page (<command>man CGI::Simple</command>) or review on CPAN: <link
        xlink:href="https://metacpan.org/pod/CGI::Simple" xmlns:xlink="http://www.w3.org/1999/xlink">CGI::Simple</link></para>

        <note>
          <para>WebDyne actually wraps the CGI::Simple object to emulate aspects of a Plack::Request handler, including supporting Hash::MultiValue parameters. For the most part this is
          transparent but something to be aware of if working with parameters which may have multiple values.</para>
        </note>

        <para>Since one of the most common code tasks is to access query parameters, WebDyne stores them in the special <varname>%_</varname> global variable before any user defined Perl
        methods are called. For example:</para>

        <para><example>
            <info>
              <title>Perl CGI access</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/cgi4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/cgi4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <note>
          <para>Values stored in the <varname>%_</varname> variable are single value only, and reflect the "last" value of any multivalue parameter supplied. The <varname>%_</varname> variable
          is a convenience for simple access - if more complex operations are required use the <classname>CGI()</classname> object.</para>
        </note>
      </section>
    </section>
  </section>

  <section xml:id="advanced_usage">
    <info>
      <title>Advanced Usage</title>
    </info>

    <para>A lot of tasks can be achieved just using the basic features detailed above. However there are more advanced features that can make life even easier</para>

    <section>
      <title>Deploying WebDyne apps with Docker</title>

      <para>After validating a WebDyne application locally, the published WebDyne container can be used as a base image for packaging the application and its Perl dependencies.</para>

      <para>The WebDyne container can be used as the basis for new docker images containing your application files. Consider the following directory structure (available from Github as <link
      xlink:href="https://github.com/aspeer/psp-WebDyne-Fortune" xmlns:xlink="http://www.w3.org/1999/xlink">aspeer/psp-WebDyne-Fortune</link>:</para>

      <screen>psp-WebDyne-Fortune/
├── app.pm
├── app.psp
├── cpanfile
├── Dockerfile
└── webdyne.conf.pl

</screen>

      <para>Where:</para>

      <variablelist>
        <varlistentry>
          <term>app.psp</term>

          <listitem>
            <para>The main and default psp file</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>app.pm</term>

          <listitem>
            <para>Perl code used in the psp file</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>cpanfile</term>

          <listitem>
            <para>A list of Perl modules to be installed in the docker container by cpanm</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>Dockerfile</term>

          <listitem>
            <para>The docker build file</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>webdyne.conf.pl</term>

          <listitem>
            <para>Any variables to be set for the WebDyne environment</para>
          </listitem>
        </varlistentry>
      </variablelist>

      <para>Constitute all the files needed to stand up a WebDyne based application in a Docker container. The contents of the Dockerfile are minimal:</para>

      <screen>FROM webdyne:latest
WORKDIR /app
# Debian packages needed for this app
RUN apt-get update &amp;&amp; apt-get -y install fortunes
COPY app.* .
COPY cpanfile .
COPY webdyne.conf.pl /etc</screen>

      <para>Build the Docker container:</para>

      <screen>docker build  -t webdyne-app-fortune -f ./Dockerfile .</screen>

      <para>And run it:</para>

      <screen>$ <command>docker run -e PORT=5010 -p 5010:5010 --name=webdyne-app-fortune webdyne-app-fortune</command></screen>

      <para>Your application should now be available:</para>

      <screenshot>
        <mediaobject>
          <imageobject>
            <imagedata fileref="images/webdyne-app-fortune1.png" scale="30"/>
          </imageobject>
        </mediaobject>
      </screenshot>
    </section>

    <section>
      <info>
        <title>Blocks</title>
      </info>

      <para>Blocks are a powerful dynamic content generation tool. WebDyne can render arbitrary blocks of text or HTML within a page, which makes generation of dynamic content generally more
      readable than similar output generated within Perl code. An example:</para>

      <para><example>
          <info>
            <title>Basic Block Rendering</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/block1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/block1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>There can be more than one block with the same name - any block with the target name will be rendered:</para>

      <para><example>
          <info>
            <title>Rendering Multiple Blocks</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/block2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/block2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>Like any other text or HTML between <tag>&lt;perl&gt;</tag> tags, blocks can take parameters to substitute into the text:</para>

      <para><example>
          <info>
            <title>Block Parameters</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/block3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/block3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>Blocks have a non-intuitive feature - they still display even if they are outside of the <tag>&lt;perl&gt;</tag> tags that made the call to render them. e.g. the following is
      OK:</para>

      <para><example>
          <info>
            <title>Blocks Outside Perl Tags</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/block4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/block4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>You can mix the two styles:</para>

      <para><example>
          <info>
            <title>Mixed Block Rendering</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/block5.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/block5.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>You can use the &lt;block&gt; tag display attribute to hide or show a block, or use a CGI parameter to determine visibility (e.g for a status update or warning):</para>

      <example>
        <info>
          <title>Conditional Block Display</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/block_toggle1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/block_toggle1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>
    </section>

    <section>
      <info>
        <title>File inclusion</title>
      </info>

      <para>You can include other file fragments at compile time using the include tag:</para>

      <para><example>
          <info>
            <title>Basic Include</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/include1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/include1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>If the file name is not an absolute path name is will be loaded relative to the directory of the parent file. For example if file "<filename>bar.psp</filename>" incorporates the
      tag <tag>&lt;include file="foo.psp"&gt;</tag> it will be expected that "<filename>foo.psp</filename>" is in the same directory as "<filename>bar.psp</filename>".</para>

      <para><important>
          <para>The include tag pulls in the target file at compile time. Changes to the included file after the WebDyne page is run the first time (resulting in compilation) are not reflected
          in subsequent output unless the <parameter>nocache</parameter> attribute is set. Thus the include tag should not be seen as a shortcut to a pseudo Content Management System. For
          example <tag>&lt;include file="latest_news.txt"&gt;</tag> may not behave in the way you expect. The first time you run it the latest news is displayed. However updating the
          "latest_news.txt" file will not result in changes to the output (it will be stale).</para>

          <para>If you do use the <parameter>nocache</parameter> attribute the included page will be loaded and parsed every time, significantly slowing down page display. There are betters
          ways to build a CMS with WebDyne - use the include tag sparingly !</para>
        </important></para>

      <para>You can include just the head or body section of a HTML or PSP file by using the head or body attributes. Here is the reference file (file to be included). It does not have to be a
      PSP file - a standard HTML file can be supplied :</para>

      <example>
        <info>
          <title>Include Source Document</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/include2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/include2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>And here is the generating file (the file that includes sections from the reference file).</para>

      <example>
        <info>
          <title>Including Head and Body Sections</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/include3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/include3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>You can also include block sections from <filename>PSP</filename> files. If this is the reference file (the file to be included) containing two blocks. This is a renderable
      <filename>PSP</filename> file in it's own right. The blocks use the <parameter>display</parameter> attribute to demonstrate that they will produce output, but it's not required:</para>

      <example>
        <info>
          <title>Include Block Source</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/include4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/include4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>And here is the file that brings in the blocks from the reference file and incorporates them into the output:</para>

      <para><example>
          <info>
            <title>Including Blocks from Another File</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/include5.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/include5.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>
    </section>

    <section xml:id="static_sections">
      <info>
        <title>Static Sections</title>
      </info>

      <para>Sometimes it is desirable to generate dynamic output in a page once only (e.g. a last modified date, a sidebar menu etc.) Using WebDyne this can be done with Perl or CGI code
      flagged with the "<property>static</property>" attribute. Any dynamic tag so flagged will be rendered at compile time, and the resulting output will become part of the compiled page - it
      will not change on subsequent page views, or have to be re-run each time the page is loaded. An example:</para>

      <para><example>
          <info>
            <title>Basic Static Section</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/static1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/static1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>In fact the above page will render very quickly because it has no dynamic content at all once the &lt;perl&gt; content is flagged as static. The WebDyne engine will recognise this
      and store the page as a static HTML file in its cache. Whenever it is called WebDyne will use the Apache <methodname>lookup_file()</methodname> or equivalent Plack function to return the
      page as if it was just serving up static content.</para>

      <para>You can check this by looking at the content of the WebDyne cache directory (usually /var/webdyne/cache). Any file with a ".html" extension represents the static version of a
      page.</para>

      <para>Of course you can still mix static and dynamic Perl sections:</para>

      <para><example>
          <info>
            <title>Mixed Static and Dynamic Sections</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/static2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/static2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>If you want the whole pages to be static, then flagging everything with the "static" attribute can be cumbersome. There is a special meta tag which flags the entire page as
      static:</para>

      <para><example>
          <info>
            <title>Page Static Meta Flag</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/static3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/static3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>If you don't like the idea of setting the static flag in meta data, then "using" the special package "WebDyne::Static" will have exactly the same effect:</para>

      <para><example>
          <info>
            <title>WebDyne::Static Package Flag</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/static3a.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/static3a.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>If the static tag seems trivial consider the example that displayed country codes:</para>

      <para><example>
          <info>
            <title>Dynamic Country List</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/cgi5.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/cgi5.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>Every time the above example is viewed the Country Name list is generated dynamically via the Locale::Country module. This is a waste of resources because the list changes very
      infrequently. We can keep the code neat but gain a lot of speed by adding the <option>static</option> tag attribute:</para>

      <para><example>
          <info>
            <title>Static Country List</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/static4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/static4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>By simply adding the "static" attribute output on a sample machine resulted in a 4x speedup in page loads. Judicious use of the static tag in places with slow changing data can
      markedly increase efficiency of the WebDyne engine.</para>
    </section>

    <section xml:id="caching_section">
      <info>
        <title>Caching</title>
      </info>

      <para>WebDyne has the ability to cache the compiled version of a dynamic page according to specs you set via the API. When coupled with pages/blocks that are flagged as static this
      presents some powerful possibilities.<important>
          <para>Caching will only work effectively if <varname>$WEBDYNE_CACHE_DN</varname> is defined and set to a directory that the web server has write access to. If caching does not work
          check that <varname>$WEBDYNE_CACHE_DN</varname> is defined and permissions set correctly for your web server.</para>
        </important></para>

      <para>There are many potential examples, but consider this one: you have a page that generates output by making a complex query to a database, which takes a lot of CPU and disk IO
      resources to generate. You need to update the page reasonably frequently (e.g. a weather forecast, near real time sales stats), but can't afford to have the query run every time someone
      view the page.</para>

      <para>WebDyne allows you to configure the page to cache the output for a period of time (say 5 minutes) before re-running the query. In this way users sees near real-time data without
      imposing a high load on the database/Web server.</para>

      <para>WebDyne knows to enable the caching code by looking for a meta tag, or by loading the <classname>WebDyne::Cache</classname> module in a <token>__PERL__</token> block.</para>

      <para>The cache code can command WebDyne to recompile a page based on any arbitrary criteria it desires. As an example the following code will recompile the page every 10 seconds. If
      viewed in between refresh intervals WebDyne will serve up the cached HTML result using Apache <code>$r-&gt;lookup_file()</code> or the FCGI equivalent, which is very fast.</para>

      <para>Try it by running the following example and clicking refresh a few times over a 20 second interval</para>

      <para><example>
          <info>
            <title>Time-Based Cache Example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/cache1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/cache1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>You can start to get more advanced in your handling of cached pages by manipulating the page UUID based on some arbitrary criteria. To extend our example above: say we have a page
      that generated sales figures for a given month. The SQL code to do this takes a long time, and we do not want to hit the database every time someone loads up the page. However we cannot
      just cache the output, as it will vary depending on the month the user chooses. We can tell the cache code to generate a different UUID based on the month selected, then cache the
      resulting output.</para>

      <para>The following example simulates such a scenario:</para>

      <para><example>
          <info>
            <title>Cache Key Example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/cache2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/cache2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para><important>
          <para>Take care when using user-supplied input to generate the page UID. There is no inbuilt code in WebDyne to limit the number of UID's associated with a page. Unless we check it,
          a malicious user could potentially DOS the server by supplying endless random "months" to the above page with a script, causing WebDyne to create a new file for each UID - perhaps
          eventually filling the disk partition that holds the cache directory. That is why we check the month is valid in the code above.</para>
        </important></para>
    </section>

    <section xml:id="dynamic_interfaces">
      <title>Dynamic Interfaces</title>

      <section>
        <title>JSON</title>

        <para>WebDyne has a &lt;json&gt; tag that can be used to present JSON data objects to Javascript libraries in an output page. Here is a very simple example:</para>

        <para><example>
            <info>
              <title>JSON Chart</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/chart1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/chart1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example>If you run it and review the source HTML you will see the JSON data rendered into the page as <tag>&lt;script&gt;&lt;/script&gt;</tag> block of type application/json with
        an id of "chartData". Any data returned by the perl routine nominated by the json tag is presented as JSON within that tag block, and available to Javascript libraries within the page.
        JSON data is kept in canonical order by default, which can be adjusted with the WEBDYNE_JSON_CANONICAL variable if not desired/needed for a very small speed-up.</para>

        <para>Another example demonstrating a table constructed with the Grid.js library</para>

        <example>
          <info>
            <title>JSON Grid</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/grid1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/grid1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>
      </section>

      <section>
        <info>
          <title>API</title>
        </info>

        <para>WebDyne has the ability to make available a basic REST API facility using the <tag>&lt;api&gt;</tag> tag in conjunction with the Router::Simple CPAN module. Documents that
        utilise the <tag>&lt;api&gt;</tag> tag are somewhat unique in that:</para>

        <itemizedlist>
          <listitem>
            <para>There is no need for any other tags in the document besides the <tag>&lt;api&gt;</tag> tag. All other tags are ignored - in fact they are discarded.</para>
          </listitem>

          <listitem>
            <para>Any PSP file file an <tag>&lt;api&gt;</tag> tag will only emit JSON data with a content type of "<type>application/json</type>"</para>
          </listitem>

          <listitem>
            <para>The REST api path must correspond to a PSP file at some path level, e.g. if your path is <filename>/api/user/42</filename> you must have a file called either
            "<filename>api.psp</filename>" or "<filename>api/user.psp</filename>" in your path.</para>
          </listitem>

          <listitem>
            <para>A PSP file can contain multiple <tag>&lt;api&gt;</tag> tags corresponding to different <classname>Router::Simple</classname> routes</para>
          </listitem>

          <listitem>
            <para>PSGI and PAGI maintain a basic in-process cache of discovered API PSP filenames to avoid repeated filesystem checks. If API filenames or API path structure are changed, a server restart may be required for the changes to be discovered.</para>
          </listitem>
        </itemizedlist>

        <para>Here is a very simple example. Note the format of the URL in the Run hyperlink:</para>

        <example>
          <info>
            <title>API Example</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/api.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/api/uppercase/bob/42" xmlns:xlink="http://www.w3.org/1999/xlink">Run uppercase API example</link></para>

          <para><link xlink:href="https://demo.webdyne.org/example/api/doublecase/bob/42" xmlns:xlink="http://www.w3.org/1999/xlink">Run doublecase API example</link></para>
        </example>

        <caution>
          <para>The <tag>&lt;api&gt;</tag> tag is still somewhat experimental and is not intended to replace a full service API handler. Use with caution</para>
        </caution>

        <note>
          <para>The REST-style API route fallback described above is implemented for the PSGI and PAGI adapters. Apache mod_perl does not currently perform this
          automatic route-prefix discovery because WebDyne is invoked only after Apache has resolved a request to a <filename>.psp</filename> file. The
          <tag>&lt;api&gt;</tag> tag can still be processed under Apache when a <filename>.psp</filename> request is explicitly routed to WebDyne, but extensionless
          API paths require suitable Apache rewrite or routing configuration.</para>
        </note>
      </section>

      <section>
        <info>
          <title>HTMX</title>
        </info>

        <para>WebDyne has support for <tag>&lt;htmx&gt;</tag> tags to supply fragmented HTML to pages using the <link xlink:href="https://htmx.org"
        xmlns:xlink="http://www.w3.org/1999/xlink">HTMX Javascript Library</link> and similar libraries such as <link xlink:href="https://alpine-ajax.js.org"
        xmlns:xlink="http://www.w3.org/1999/xlink">Alpine Ajax</link>. WebDyne can support just supplying HTML snippet to pages in response to htmx or similar calls. HTMX/Alpine Ajax and
        WebDyne are complementary libraries which can be combined together to support dynamic pages with in-place updates from a WebDyne Perl backend. <important>
            <para>The htmx javascript library must be activated in any file that utilises htmx calls (initiates a <command>hx-get</command> or similar operation). The javascript can be loaded
            in any convenient way, via a <tag>&lt;start_html&gt;</tag> <property>script</property> attribute, traditional <tag>&lt;script&gt;</tag> tags etc.</para>
          </important></para>

        <para>Here is a simple HTML file (<filename>htmx_demo1.psp</filename>) incorporating HTMX calls to a backend file called <filename>htmx_time1.psp</filename>. Here is the display file,
        <filename>htmx_demo1.psp</filename></para>

        <para><example>
            <info>
              <title>HTMX Display Page</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/htmx_demo1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/htmx_demo1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example>And the backend file which generates the HTMX data for the above page (<filename>htmx_time1.psp</filename>):</para>

        <example>
          <info>
            <title>HTMX Backend Page</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/htmx_time1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/htmx_time1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example>

        <para>Note the <tag>&lt;htmx&gt;</tag> tags. You can run the above htmx resource file and it will render correctly as a full HTML page - however if WebDyne detects a
        '<property>hx-request</property>' HTTP header it will only send the fragment back.</para>

        <important>
          <para>Only one <tag>&lt;htmx&gt;</tag> section from a file will ever be rendered. You can have multiple <tag>&lt;htmx&gt;</tag> sections in a PSP file however only one can be
          rendered at any time. You can use the display attribute with dynamic matching (see later) to render different <tag>&lt;htmx&gt;</tag> sections in a PSP file, or you can keep them all
          in different files (e.g. one <tag>&lt;htmx&gt;</tag> section per PSP file</para>
        </important>

        <important>
          <para>Content in <tag>&lt;htmx&gt;</tag> tags will not be rendered unless a <property>hx-request</property> HTTP header is detected, or (as in the above example) the force attribute
          is nominated.</para>
        </important>

        <section>
          <info>
            <title>Using Perl within &lt;htmx&gt; tags</title>
          </info>

          <para><tag>&lt;htmx&gt;</tag> tags can be called with the same attributes as <tag>&lt;perl&gt;</tag> tags, including nominating a handler to generate data. See the following
          example:</para>

          <para><example>
              <info>
                <title>HTMX Handler Display Page</title>
              </info>

              <para><programlisting language="html"><xi:include href="example/htmx_demo2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
              xlink:href="https://demo.webdyne.org/example/htmx_demo2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
            </example>And the backend file which generates the HTMX data for the above page - now with the HTML fragment generated by Perl:<example>
              <info>
                <title>HTMX Handler Backend Page</title>
              </info>

              <para><programlisting language="html"><xi:include href="example/htmx_time2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
              xlink:href="https://demo.webdyne.org/example/htmx_time2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
            </example></para>
        </section>

        <section>
          <title>Using multiple <tag>&lt;htmx&gt;</tag> tags in one PSP file</title>

          <para>As is mentioned above only one <tag>&lt;htmx&gt;</tag> fragment can be returned by a PSP page at a time - but you can use techniques to select which tag should be rendered. The
          <tag>&lt;htmx&gt;</tag> tag supports the display attribute. If this attribute exists and is a "true" value then the <tag>&lt;htmx&gt;</tag> fragment will be returned. At first this
          doesn't seem very useful - but when combined with dynamic evaluation via either page query parameters or <code>!{! .. !}</code> evaluation it becomes more compelling. Take the
          following two button example:</para>

          <para><example>
              <info>
                <title>Multiple HTMX Fragment Display Page</title>
              </info>

              <para><programlisting language="html"><xi:include href="example/htmx_demo3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
              xlink:href="https://demo.webdyne.org/example/htmx_demo3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
            </example><important>
              <para>WebDyne does not support single quoted tags - you can use them in PSP files, but the rendered HTML will always emit tag attributes as double quoted. For attributes such as
              <property>hx-vals</property> it is not possible to supply JSON strings within single quotes, i.e. this will not work: <tag>&lt;button hx-vals='{ "foo" : "bar" }'&gt;</tag> - the
              single quotes will be emitted as double quotes, causing the browser to misinterpret the tag. If you need to supply quoted keys uses the <code>js:{}</code> syntax above.</para>
            </important>Here is the HTML page used by the above example to fetch the requested time. You can run it, however nothing will be emitted unless you supply a
          <code>?time_local=1</code> or <code>?time_utc=1</code> query parameter in your browser.<example>
              <info>
                <title>Multiple HTMX Fragment Backend Page</title>
              </info>

              <para><programlisting language="html"><xi:include href="example/htmx_time3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
              xlink:href="https://demo.webdyne.org/example/htmx_time3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
            </example>Normally you would expect to have the hx-get attribute for each button go to a different PSP page. But in this instance they refer to the same page. So how do we
          discriminate ? The key is in the supply of the hx-vals attribute, which allows us to send query strings to the htmx resource page. We can then use them to select which &lt;htmx&gt;
          block is returned.</para>

          <note>
            <para>Note the use of <code>js:{ &lt;json&gt; }</code> notation in the <tag>&lt;htmx&gt;</tag> <parameter>hx-vals</parameter> attribute. It allows for easier supply of JSON data
            without needed to manipulate/escape double-quotes in raw JSON data. You'll also note there is no <tag>&lt;start_html&gt;</tag> tag. It's not necessary for <tag>&lt;htmx&gt;</tag>
            pages.</para>
          </note>
        </section>

        <section>
          <title>Putting everything in one file</title>

          <para>Because &lt;htmx&gt; tag does not render unless a hx-request header is received we can serve htmx content from the same PSP file that calls it. Here is an example that brings
          everything into one file:</para>

          <example>
            <info>
              <title>Single-File HTMX Resource</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/htmx_time4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/htmx_time4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example>

          <para>Or the local/UTC time example converted to run everything from one file:</para>

          <example>
            <info>
              <title>Single-File HTMX Demo</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/htmx_demo4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
            xlink:href="https://demo.webdyne.org/example/htmx_demo4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example>
        </section>
      </section>

      <section>
        <title>Server-Sent Events (SSE)</title>

        <para>When run in a PAGI environment WebDyne supports Server-Sent Events (SSE). An async <function>sse</function> subroutine can be defined which will be called when an SSE event
        stream is requested. The handler can access the underlying <classname>PAGI::SSE</classname> helper via <code>$self-&gt;r-&gt;sse()</code>.</para>

        <para>SSE pages normally declare their event handler through the <tag>&lt;start_html&gt;</tag> tag. The bare <property>sse</property> attribute is equivalent to
        <property>sse=sse</property>, so WebDyne will call an async subroutine named <function>sse</function>. To use a different handler name, supply it as the attribute value, for example
        <tag>&lt;start_html sse="my_sse_handler"&gt;</tag>.</para>

        <para>The following example also uses <property>h3</property>, <property>hr</property> and <property>alpine</property> on <tag>&lt;start_html&gt;</tag>. The heading and rule attributes
        are syntactic shortcuts for simple page formatting, while <property>alpine</property> is a shortcut that loads the Alpine.js JavaScript library. See the <tag>&lt;start_html&gt;</tag>
        tag reference for the complete list of shortcuts and options. Longer form HTML, such as writing a normal <tag>&lt;script&gt;</tag> tag to load a JavaScript library yourself, is still
        supported.</para>

        <para><example>
            <info>
              <title>SSE</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/sse1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting></para>

            <para><link xlink:href="https://demo.webdyne.org/example/progress.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
          </example></para>

        <para>If you do not wish to use the <tag>&lt;start_html&gt;</tag> tag with the sse attribute you can use a meta tag to denote which subroutine to call for SSE events, e.g.
        <tag>&lt;meta name="WebDyne" content="sse=sse"&gt;</tag>.</para>
      </section>

      <section>
        <title>WebSockets</title>

        <para>When run in a PAGI environment WebDyne supports WebSockets (WS). An async <function>ws</function> subroutine can be defined which will be called when a PAGI WebSocket connection
        is opened. The request object exposes the underlying PAGI scope and send/receive callbacks, and the handler can use those to accept the connection and exchange WebSocket messages.</para>

        <para>WebSocket pages normally declare their event handler through the <tag>&lt;start_html&gt;</tag> tag. The bare <property>ws</property> attribute is equivalent to
        <property>ws=ws</property>, so WebDyne will call an async subroutine named <function>ws</function>. To use a different handler name, supply it as the attribute value, for example
        <tag>&lt;start_html ws="my_ws_handler"&gt;</tag>.</para>

        <para>As with SSE pages, <tag>&lt;start_html&gt;</tag> can also use formatting and framework shortcuts such as <property>h3</property>, <property>hr</property> and
        <property>alpine</property>. The <property>alpine</property> shortcut loads the Alpine.js JavaScript library; normal HTML <tag>&lt;script&gt;</tag> tags can still be used instead when
        you want to load JavaScript in the traditional manner. See the <tag>&lt;start_html&gt;</tag> tag reference for details.</para>

        <para><example>
            <info>
              <title>WebSockets</title>
            </info>

            <para><programlisting language="html"><xi:include href="example/ws1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting></para>
          </example></para>

        <para><link xlink:href="https://demo.webdyne.org/example/ws.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>

        <para>If you do not wish to use the <tag>&lt;start_html&gt;</tag> tag with the ws attribute you can use a meta tag to denote which subroutine to call for WebSocket events, e.g.
        <tag>&lt;meta name="WebDyne" content="ws=ws"&gt;</tag>.</para>
      </section>
    </section>
  </section>

  <section xml:id="error_handling">
    <info>
      <title>Error Handling</title>
    </info>

    <section xml:id="dump_tag">
      <info>
        <title>Dump</title>
      </info>

      <para>The <tag>&lt;dump&gt;</tag> tag is a informational element which can be included in a page for diagnostic or debugging purposes. It will show various variable and state values for
      the page. By default if a <tag>&lt;dump&gt;</tag> flag is embedded in a page diagnostic information is not shown unless the <parameter>force</parameter> attribute is specified or the
      <constant>$WEBDYNE_DUMP_FLAG</constant> is set, the latter allowing the <tag>&lt;dump&gt;</tag> tag to be embedded into all pages on a site but not activated unless debugging
      enabled.</para>

      <para>Various diagnostic elements can be displayed - see the &lt;dump&gt; tag section for information on what they are. In this example all components are enabled and display is
      forced:</para>

      <example>
        <info>
          <title>Forced Dump Output</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/dump1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/dump1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>Dump display/hide can be controlled by form parameters or run URI query strings. In the example below ticking the checkbox or simply appending "?dump_enable=1" to the URL will
      display the dump information:</para>

      <example>
        <info>
          <title>Conditional Dump Output</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/dump2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
        xlink:href="https://demo.webdyne.org/example/dump2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>
    </section>

    <section>
      <info>
        <title>Error Messages</title>
      </info>

      <para>Sooner or later something is going to go wrong in your code. If this happens WebDyne will generate an error showing what the error was and attempting to give information on where
      it came from: Take the following example:</para>

      <para><example>
          <info>
            <title>Error handling</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/err1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/err1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>If you run the above example an error message will be displayed</para>

      <screenshot>
        <mediaobject>
          <imageobject>
            <imagedata fileref="images/err1.png" scale="50"/>
          </imageobject>
        </mediaobject>
      </screenshot>

      <para>In this example the backtrace is within in-line code, so all references in the backtrace are to internal WebDyne modules. The code fragment will show the line with the
      error.</para>

      <para>If we have a look at another example:</para>

      <para><example>
          <info>
            <title>Intentional die()</title>
          </info>

          <para><programlisting language="html"><xi:include href="example/err2.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting><link
          xlink:href="https://demo.webdyne.org/example/err2.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
        </example></para>

      <para>And the corresponding screen shot:</para>

      <screenshot>
        <mediaobject>
          <imageobject>
            <imagedata fileref="images/err2.png" scale="50"/>
          </imageobject>
        </mediaobject>
      </screenshot>

      <para>We can see that the error occurred in the "hello" subroutine (invoked at line 2 of the page) within the perl block on line 9. The 32 digit hexadecimal number is the page unique ID
      - it is different for each page. WebDyne runs the code for each page in a package name space that includes the page's UID - in this way pages with identical subroutine names (e.g. two
      pages with a "hello" subroutine) can be accommodated with no collision.</para>
    </section>

    <section>
      <info>
        <title>Exceptions</title>
      </info>

      <para>Errors (exceptions) can be generated within a WebDyne page in two ways:</para>

      <itemizedlist>
        <listitem>
          <para>By calling die() as shown in example above.</para>
        </listitem>

        <listitem>
          <para>By returning an error message via the err() method, exported by default.</para>
        </listitem>
      </itemizedlist>

      <para>Examples</para>

      <para><programlisting>__PERL__


#  Good
#
sub hello {

    return err('no foobar') if !$foobar;

}

# Also OK
#
sub hello {

    return die('no foobar') if !$foobar;

}</programlisting></para>
    </section>

    <section>
      <info>
        <title>Error Checking</title>
      </info>

      <para>So far all the code examples have just assumed that any call to a WebDyne API method has been successful - no error checking is done. WebDyne always returns "undef" if an API
      method call fails - which should be checked for after every call in a best practice scenario.</para>

      <example>
        <info>
          <title>Error checking</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/err3.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting> <link
        xlink:href="https://demo.webdyne.org/example/err3.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>You can use the err() function to check for errors in WebDyne Perl code associated with a page, e.g.:</para>

      <example>
        <info>
          <title>Error throw</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/err4.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting></para>

        <para><link xlink:href="https://demo.webdyne.org/example/err4.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>Note that the backtrace in this example shows where the error was triggered from.</para>
    </section>
  </section>

  <section xml:id="utilities">
    <info>
      <title>Utilities</title>
    </info>

    <section xml:id="command_line_utilities">
      <info>
        <title>Command Line Utilities</title>
      </info>

      <para>WebDyne installs several command line utilities for setup, local development and troubleshooting. Installation location will vary depending on your distribution - most will default
      to <filename>/usr/local/bin</filename>, but may be installed elsewhere in some cases, especially if you have nominated a <option>PREFIX</option> option when using CPAN. The commands
      provide their own help or manual output for detailed options.</para>

      <variablelist>
        <varlistentry>
          <term><command>wdapacheinit</command></term>

          <listitem>
            <para>Runs the WebDyne initialization routines, which create needed directories, modify and create Apache .conf files etc.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/wdapacheinit.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>wdcompile</command></term>

          <listitem>
            <para>Usage: <command>wdcompile filename.psp</command>. Will compile a PSP file and use Data::Dumper to display the WebDyne internal representation of the page tree structure.
            Useful as a troubleshooting tool to see how HTML::TreeBuilder has parsed your source file, and to show up any misplaced tags etc.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/wdcompile.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>wdrender</command></term>

          <listitem>
            <para>Usage: <command>wdrender filename.psp</command>. Will attempt to render the source file to screen using WebDyne. It is useful for testing page output from the command line
            and can exercise fake, PSGI, PAGI and mod_perl style request backends for troubleshooting.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/wdrender.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>wddump</command></term>

          <listitem>
            <para>Usage: <command>wddump filename</command>. Where filename is a compiled WebDyne source file (usually in <filename>/var/webdyne/cache</filename>). Will dump out the saved data
            structure of the compiled file.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/wddump.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>wddebug</command></term>

          <listitem>
            <para>Usage: <command>wddebug --status|--enable|--disable</command>. Enable/disable debugging in the WebDyne code. This uses some pretty ugly methods to enable debugging in already
            installed modules by editing the code on-disk to re-enable debug calls - do not use in a production environment !</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/wddebug.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>webdyne.psgi</command></term>

          <listitem>
            <para>Used to run WebDyne as a PSGI process- usually invoked by Plack via <command>plackup</command> or <command>starman</command>, but can be run directly for development
            purposes.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/webdyne.psgi.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>webdyne.pagi</command></term>

          <listitem>
            <para>Used to run WebDyne as a PAGI application. It can be run directly for development or used as a PAGI application entry point, including PAGI HTTP, SSE and WebSocket request
            handling where supported by the server.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/webdyne.pagi.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>webdyne.apache</command></term>

          <listitem>
            <para>Starts a temporary Apache mod_perl instance for local development and troubleshooting. It can serve a directory or a single PSP file without installing a full system Apache
            configuration.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/webdyne.apache.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><command>wdlint</command></term>

          <listitem>
            <para>Run <command>perl -c -w</command> over code in <token>__PERL__</token> sections on any PSP file to check for syntax errors. Will automatically skip HTML code. It only checks
            code in the <token>__PERL__</token> area, and won't check syntax in in-line perl, dynamic attributes etc.</para>

            <para><link role="manual-page-new-tab" xlink:href="utilities/wdlint.md" xmlns:xlink="http://www.w3.org/1999/xlink">Full manual page</link></para>
          </listitem>
        </varlistentry>
      </variablelist>
    </section>
  </section>

  <section xml:id="modules">
    <info>
      <title>Modules</title>
    </info>

    <para>The following module reference pages are generated from markdown sidecar files maintained beside the Perl modules in <filename>lib</filename>. They are copied into the MkDocs tree
    during documentation generation so the source module documentation only needs to be maintained in one place.</para>

    <itemizedlist>
      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_CGI.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::CGI</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_CGI_Simple.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::CGI::Simple</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Cache.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Cache</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Chain.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Chain</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Chain_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Chain::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Compile.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Compile</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Constant.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Err.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Err</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Err_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Err::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Filter.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Filter</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Filter_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Filter::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_HTML_Tiny.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::HTML::Tiny</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_HTML_TreeBuilder.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::HTML::TreeBuilder</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Handler.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Handler</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Install.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Install</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Install_Apache.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Install::Apache</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Install_Apache_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Install::Apache::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Install_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Install::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_PAGI.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::PAGI</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_PAGI_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::PAGI::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_PSGI.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::PSGI</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_PSGI_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::PSGI::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Request_Apache.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Request::Apache</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Request_Common.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Request::Common</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Request_Fake.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Request::Fake</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Request_PAGI.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Request::PAGI</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Request_PSGI.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Request::PSGI</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Request_PSGI_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Request::PSGI::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Request_PSGI_Static.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Request::PSGI::Static</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Session.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Session</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Session_Constant.md"
        xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Session::Constant</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Static.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Static</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Template.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Template</classname></link></para>
      </listitem>

      <listitem>
        <para><link role="manual-page-new-tab" xlink:href="modules/WebDyne_Util.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Util</classname></link></para>
      </listitem>
    </itemizedlist>
  </section>

  <section xml:id="reference">
    <info>
      <title>Reference</title>
    </info>

    <section xml:id="tag_reference">
      <info>
        <title>Tag Reference</title>
      </info>

      <para>Reference of WebDyne tags and supported attributes</para>

      <section xml:id="tag_reference_core">
        <info>
          <title>Core Tags</title>
        </info>

        <section xml:id="tag_perl">
          <info>
            <title><tag>&lt;perl&gt;</tag></title>
          </info>

          <para>Run Perl code either in-line (between the <tag>&lt;perl&gt;..&lt;/perl&gt;</tag>) tags, or non-inline via the subroutine/method nominated by the <property>handler</property>
          attribute. If this tag is invoked without a <property>handler</property> attribute, text between the tags will be interpreted as perl code and executed. If invoked with a
          <tag>handler</tag> attribute, text between the tags will be interpreted as a template - which can be output by a call to the WebDyne <methodname>render()</methodname> method within
          the handler.</para>

          <programlisting language="html">&lt;perl
  [handler=METHOD]
  [require=MODULE | FILE]
  [import=FUNCTION [, FUNCTION ...]]
  [param=SCALAR | HASHREF]
  [run]
  [static]
  [file]
  [hidden]
  [display=0]
  [chomp]
  [autonewline]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>handler=METHOD</term>

              <listitem>
                <para>Call an external Perl method from a module, or a subroutine in the <token>__PERL__</token> block at the end of the PSP file. If the handler is specified as "fully
                qualified" module call (e.g. <classname>Digest::MD5::md5_hex()</classname>) then a <methodname>require</methodname> will be made automatically to load the module
                (<classname>Digest::MD5</classname> in this example)</para>

                <programlisting language="html">#  Call method in the same file
#
&lt;perl handler="hello"&gt;
__PERL__
sub hello {
...
}

#  Call method in another class
#
&lt;perl handler="Digest::MD5::md5_hex()"&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>require=MODULE | FILE</term>

              <listitem>
                <para>Load a Perl module or file needed to support a method call. E.g. <tag>&lt;perl require="Digest::MD5"/&gt;</tag> to load the <classname>Digest::MD5</classname> module.
                Anything with a [./\] character is treated as a file path to a Perl file (e.g. "<filename>/home/user/module.pm</filename>"), otherwise it is treated as module name
                ("<classname>Digest::MD5</classname>")</para>

                <programlisting language="html">#  Load a module
#
&lt;perl require="Digest::MD5"&gt;

#  Load a file
#
&lt;perl require="module.pm"&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>import=FUNCTION [, FUNCTION …]</term>

              <listitem>
                <para>Import a single or multiple functions into the file namespace. Use a single SCALAR for importing one function, or pass an ARRAY reference for multiple functions.</para>

                <programlisting language="html"># Import single function
#
&lt;perl require="Digest::MD5" import="md5_hex"&gt;

# Import multiple functions
#
&lt;perl require="Digest::MD5" import="@{'md5_hex', 'md5_base64'}"&gt;
&lt;perl require="Digest::MD5" import="@{qw(md5_hex md5_base64)}"&gt;</programlisting>

                <para>Imported methods available anywhere in the namespace of that page.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>param=SCALAR | HASHREF</term>

              <listitem>
                <para>Parameters to be supplied to perl routine, can be a single SCALAR value (string, numeric etc.) or a HASH reference.</para>

                <programlisting language="html">#  Pass parameters to a handler. Single parameter
#
&lt;perl handler="hello" param="Bob"&gt;

#  Pass hash ref
#
&lt;perl handler="hello" param="%{ name=&gt; 'Bob', age =&gt; 42 }"&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>static</term>

              <listitem>
                <para>Boolean flag. The Perl code is run once only and the output cached for all subsequent requests. If omitted the code is not cached (i.e. it is run each time).</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>run</term>

              <listitem>
                <para>Boolean flag. If evaluated to a true value exists the code is run, if not the code is skipped. If omitted the code is run by default. Useful for conditional running of
                code when a form has been submitted or a particular logic threshold reached.</para>

                <programlisting language="html">#  Run code only at 4am
#
&lt;perl handler="banner" run="(localtime)[2] == 4"&gt;

#  Run code only if a "name" form parameter supplied, all below equivalent
#
&lt;perl handler="hello" run="+{name}"&gt; ...
&lt;perl handler="hello" run="!{! exists $_{'name'} !}"&gt; ...
&lt;perl handler="hello" run="!{! defined shift()-&gt;CGI-&gt;param('name') !}</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>file</term>

              <listitem>
                <para>Boolean flag. Force package|require attribute value to be treated as a file, even if it appears to "look like" a module name to the loader. Rarely needed, use case would
                be a Perl module in the current directory without an extension.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>hidden</term>

              <listitem>
                <para>Boolean flag. The output from the Perl module will be hidden and not rendered to the page.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>display=0</term>

              <listitem>
                <para>Equivalent to setting hidden attribute.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>method=METHOD</term>

              <listitem>
                <para>Compatibility alias for <property>handler</property>. Prefer <property>handler</property> in new documentation and examples.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>package=PACKAGE</term>

              <listitem>
                <para>Optional package component used when constructing a fully qualified handler call. Prefer a fully qualified <property>handler</property> value unless separating the package
                and method names is useful for compatibility with older PSP files.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>chomp</term>

              <listitem>
                <para>Boolean flag. Any new lines at the end of the output will be truncated.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>autonewline</term>

              <listitem>
                <para>Boolean flag. A newline character will be inserted between each <methodname>print</methodname> statement.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_json">
          <info>
            <title><tag>&lt;json&gt;</tag></title>
          </info>

          <para>Run Perl code similar to <tag>&lt;perl&gt;</tag> tag but expects code to return a HASH, ARRAY ref or plain scalar, which is encoded into JSON, outputting within a
          &lt;script&gt; tag with <property>type="application/json"</property>. When supplied with an id attribute this data can be used by any Javascript function in the page. Takes the same
          options as the <tag>&lt;perl&gt;</tag> tag and behaves similarly - if a <property>handler</property> attribute is given it is called, if the <property>perl</property> attribute is
          given text between the <tag>&lt;json&gt;</tag> tags in treated as in-line perl code and executed.</para>

          <programlisting language="html">&lt;json
  [id=NAME]
  [handler=METHOD]
  [pretty]
  [canonical]
  [perl]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>id=NAME</term>

              <listitem>
                <para>the DOM ID the <tag>&lt;script&gt;</tag> tag output from the tag will be given, e.g. <code>&lt;script id="mydata"
                type="application/json"&gt;{"foo":1}&lt;/script&gt;</code></para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>pretty</term>

              <listitem>
                <para>Boolean flag. Use the JSON <methodname>pretty()</methodname> method to format the output data into something more human readable. Not enabled by default. Enable with
                <property>pretty=1</property> attribute or globally via <varname>$WEBDYNE_JSON_PRETTY=1</varname> configuration setting.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>canonical</term>

              <listitem>
                <para>Boolean flag. Use the JSON <methodname>canonical()</methodname> method to sort JSON data. Enabled by default, disable using <property>canonical=0</property> attribute
                value or via <varname>$WEBDYNE_JSON_CANONICAL=0</varname> configuration setting.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>perl</term>

              <listitem>
                <para>Interpret content between starting and ending <tag>&lt;json&gt;</tag> tag as perl code and run it. The code should return a HASH, ARRAY or BOOLEAN value which will then
                be encoded to JSON data.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>handler=METHOD</term>

              <listitem>
                <para>Call the perl method nominated. The code should return a HASH, ARRAY or BOOLEAN value which will then be encoded to JSON data.</para>
              </listitem>
            </varlistentry>
          </variablelist>

          <note>
            <para>If returning JSON boolean values in code you should use the JSON::true and JSON::false values rather than 0 or 1, e.g.</para>

            <programlisting language="html">&lt;start_html&gt;
&lt;json handler/&gt;
...
__PERL__
use JSON:
sub handler {
    return { enabled =&gt; JSON::true }
}</programlisting>
          </note>
        </section>

        <section xml:id="tag_block">
          <info>
            <title><tag>&lt;block&gt;</tag></title>
          </info>

          <para>Block of HTML code to be optionally rendered if desired by call to <methodname>render_block()</methodname> Webdyne method:</para>

          <programlisting language="html">&lt;block
  name=NAME
  [display]
  [static]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>name=NAME</term>

              <listitem>
                <para><emphasis>Mandatory.</emphasis> The name for this block of PSP or HTML. Referenced when rendering a particular block within perl code, e.g. <code>return
                $self-&gt;render_block("foo")</code></para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>display</term>

              <listitem>
                <para>Boolean flag. Force display of this block even if not invoked by <methodname>render_block()</methodname> method in handler. Useful for prototyping or conditional display.
                Any true value will force display, so this can be coupled with a form parameter to only show a block when a form has been submitted in a similar form to the
                <tag>&lt;perl&gt;</tag> tag <property>run</property> attribute.</para>

                <programlisting language="html">#  Only show a block if a name parameter has been supplied
#
&lt;block name="showname" display="+{name}"&gt;
Thank you for registering +{name} !
&lt;/block&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>static</term>

              <listitem>
                <para>Boolean flag. This block is rendered once only and the output cached for all subsequent requests</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_include">
          <info>
            <title><tag>&lt;include&gt;</tag></title>
          </info>

          <para>Include HTML, PSP or text from an external file. Can pull in just the <tag>&lt;head&gt;</tag>, <tag>&lt;body&gt;</tag> or a <tag>&lt;block&gt;</tag> section from another HTML
          or PSP file. If pulled in from a PSP file it will be compiled and interpreted in the context of the current page.</para>

          <programlisting language="html">&lt;include
  file=PATH | ARRAYREF
  [block=NAME]
  [wrap=TAG]
  [head]
  [body]
  [param=HASHREF]
  [nocache]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>file=PATH | ARRAYREF</term>

              <listitem>
                <para><emphasis>Mandatory</emphasis>. Name of file or files we want to include. Can be relative to current directory or absolute path.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>head</term>

              <listitem>
                <para>Boolean flag. File is an HTML or PSP file and we want to include just the <tag>&lt;head&gt;</tag> section</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>body</term>

              <listitem>
                <para>Boolean flag/ File is an HTML or PSP file and we want to include just the <tag>&lt;body&gt;</tag> section.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>block=NAME</term>

              <listitem>
                <para>File is a PSP file and we want to include a <tag>&lt;block&gt;</tag> section from that file with the nominated name.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>wrap=TAG</term>

              <listitem>
                <para>Wrap the text from a plain file include in the nominated tag. Do not use &lt;&gt; symbols, just the plain tag name. This option applies when including a file directly, not
                when extracting <property>head</property>, <property>body</property>, or <property>block</property> content from another HTML or PSP file.</para>

                <programlisting language="html">#  Include the protocols file and wrap in &lt;pre&gt;
#
&lt;include="/etc/protocols" wrap="pre"&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>param=HASHREF</term>

              <listitem>
                <para>Parameter hash supplied while rendering included PSP content. This is used when extracting a <property>head</property>, <property>body</property>, or named
                <property>block</property> from another PSP file.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>nocache</term>

              <listitem>
                <para>Don't cache the results of the include, bring them in off disk each time. Will incur performance penalty</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_api">
          <info>
            <title><tag>&lt;api&gt;</tag></title>
          </info>

          <para>Respond to a JSON request made from a client. Takes the same options as the <tag>&lt;perl&gt;</tag> tag and behaves similarly - if a <property>handler</property> attribute is
          given it is called, if the <property>perl</property> attribute is given the text between the <tag>&lt;api&gt;</tag> tags is treated as in-line perl code and executed. Responses from
          perl code are encoded as JSON and returned.</para>

          <programlisting language="html">&lt;api
  pattern=ROUTE
  [match=ROUTE]
  [destination=HASHREF | dest=HASHREF | data=HASHREF]
  [option=HASHREF | options=HASHREF | constraint=HASHREF | constraints=HASHREF]
  [canonical]
&gt;</programlisting>

          <para><variablelist>
              <varlistentry>
                <term>pattern=ROUTE</term>

                <listitem>
                  <para><emphasis>Mandatory</emphasis>. Name of <classname>Router::Simple</classname> pattern we want to serve, e.g. /api/{user}/:id. The <property>match</property>
                  attribute is accepted as an alias.</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>destination=HASHREF | dest=HASHREF | data=HASHREF</term>

                <listitem>
                  <para>Hash we want to supply to perl routine if match made. See <classname>Router::Simple</classname></para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>option=HASHREF | options=HASHREF | constraint=HASHREF | constraints=HASHREF</term>

                <listitem>
                  <para>Match options, GET, PUT etc. <classname>Router::Simple</classname></para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>canonical</term>

                <listitem>
                  <para>Boolean flag. Use the JSON <methodname>canonical()</methodname> method to sort JSON response data. Enabled by default, disable using <property>canonical=0</property>
                  attribute value or via <varname>$WEBDYNE_JSON_CANONICAL=0</varname> configuration setting.</para>
                </listitem>
              </varlistentry>
            </variablelist></para>
        </section>

        <section xml:id="tag_htmx">
          <info>
            <title><tag>&lt;htmx&gt;</tag></title>
          </info>

          <para>Serve HTML fragments in response to <link xlink:href="https://htmx.org" xmlns:xlink="http://www.w3.org/1999/xlink">htmx</link> type requests (or similar clients). Takes the
          same options as the <tag>&lt;perl&gt;</tag> tag and behaves similarly - if a <property>handler</property> attribute is given it is called, if the <property>perl</property> attribute
          is given the text between the <tag>&lt;htmx&gt;</tag> tags is treated as in-line perl code and executed.</para>

          <programlisting language="html">&lt;htmx
  [handler=METHOD]
  [perl]
  [display]
  [force]
&gt;</programlisting>

          <para><variablelist>
              <varlistentry>
                <term>display</term>

                <listitem>
                  <para>Boolean. If evaluates to true then this <tag>&lt;htmx&gt;</tag> snippet fires. You can have multiple htmx tag sections in a page, but only one can fire at a time. Use
                  this attribute in conjunction with dynamic evaluation</para>

                  <programlisting language="html">#  Fire htmx tag only if a name parameter matches
#
&lt;htmx display="!{! $_{name} eq 'Bob' !}"&gt;
Hello Bob
&lt;/htmx&gt;


#  Or Alice. Both tags can live in same document as only one will ever fire
#
&lt;htmx display="!{! $_{name} eq 'Alice' !}"&gt;
Hello Alice
&lt;/htmx&gt;</programlisting>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>force</term>

                <listitem>
                  <para>Boolean. Force the code referenced by a <tag>&lt;htmx&gt;</tag> tag to run, and content be returned/displayed even if the request is not triggered by the htmx
                  javascript module (which is determined by looking for a <varname>hx-request</varname> HTTP header). Useful for troubleshooting/debugging and/or showing what the generated
                  HTML snippet will look like. Can be dynamic and be triggered by GET parameter:</para>

                  <programlisting language="html">&lt;htmx force="+{debug}"&gt;
This is my output
&lt;/htmx&gt;</programlisting>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>perl</term>

                <listitem>
                  <para>Boolean. Interpret content between starting and ending <tag>&lt;htmx&gt;</tag> tags as perl code and run it. Anything returned by the perl code will be sent as the HTML
                  fragment.</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>handler=METHOD</term>

                <listitem>
                  <para>Call the perl method nominated. Whatever is returned or rendered by the handler will be returned as the HTML fragment.</para>
                </listitem>
              </varlistentry>
            </variablelist></para>
        </section>

        <section xml:id="tag_dump">
          <info>
            <title><tag>&lt;dump&gt;</tag></title>
          </info>

          <para>Display CGI and other parameters in <classname>Data::Dumper</classname> dump format. Useful for debugging. Only rendered if <varname>$WEBDYNE_DUMP_FLAG</varname> global set to
          1 in WebDyne constants or the <property>display|force</property> attribute specified (see below). Useful while troubleshooting or debugging pages.</para>

          <programlisting language="html">&lt;dump
  [display]
  [force]
  [all]
  [cgi]
  [env]
  [lib | inc]
  [constant]
  [dir_config]
  [version]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>display|force</term>

              <listitem>
                <para>Boolean. Force display even if <varname>$WEBDYNE_DUMP_FLAG</varname> global not set</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>all</term>

              <listitem>
                <para>Boolean. Display all diagnostic blocks</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>cgi</term>

              <listitem>
                <para>CGI parameters and query strings are always displayed once dump output is enabled.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>env</term>

              <listitem>
                <para>Boolean. Display environment variables</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>lib | inc</term>

              <listitem>
                <para>Boolean. Display the Perl include paths and loaded module table (<varname>@INC</varname> and <varname>%INC</varname>).</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>constant</term>

              <listitem>
                <para>Boolean. Display WebDyne configuration constants.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>dir_config</term>

              <listitem>
                <para>Boolean. Display WebDyne request directory configuration.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>version</term>

              <listitem>
                <para>Version strings are always displayed once dump output is enabled.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_start_html">
          <info>
            <title><tag>&lt;start_html&gt;</tag></title>
          </info>

          <para>Start a HTML page with all conventional tags. This will produce the output:<programlisting language="html"><tag>&lt;html&gt;&lt;head&gt;&lt;title&gt;&lt;/title&gt;&lt;meta&gt;&lt;/head&gt;&lt;body&gt;</tag></programlisting>
          with appropriate content attributes as output.</para>

          <programlisting language="html">&lt;start_html
  [title=TEXT]
  [meta=HASHREF]
  [style=URL | ARRAYREF]
  [style_prepend=URL | ARRAYREF]
  [style_append=URL | ARRAYREF]
  [script=URL | ARRAYREF]
  [script_prepend=URL | ARRAYREF]
  [script_append=URL | ARRAYREF]
  [base=URL]
  [target=TARGET]
  [author=EMAIL]
  [include=PATH | ARRAYREF]
  [include_script=PATH | ARRAYREF]
  [include_style=PATH | ARRAYREF]
  [static]
  [cache=METHOD]
  [handler=METHOD]
  [h1 | h2 | h3 | h4 | h5 | h6]
  [hr]
  [sse=METHOD]
  [ws=METHOD]
  [pico]
  [htmx]
  [alpine]
&gt;</programlisting>

          <para>Any attributes not listed here are passed through to the generated <tag>&lt;html&gt;</tag> tag after WebDyne has consumed the page-control attributes. The shortcut attributes
          <parameter>pico</parameter>, <parameter>htmx</parameter>, and <parameter>alpine</parameter> are defined by <varname>WEBDYNE_START_HTML_SHORTCUT_HR</varname> and can be changed or
          extended in WebDyne configuration.</para>

          <para>Values in <varname>WEBDYNE_START_HTML_PARAM</varname> are applied before attributes from the page <tag>&lt;start_html&gt;</tag> tag. If a page supplies the same attribute with a
          value, the page value replaces the configured value for that attribute. For example, <property>style</property> replaces a configured <property>style</property> list, and
          <property>script</property> replaces a configured <property>script</property> list. Use <property>style_prepend</property>, <property>style_append</property>,
          <property>script_prepend</property>, or <property>script_append</property> when a page should add resources around configured defaults instead of replacing them.</para>

          <variablelist>
            <varlistentry>
              <term>title=TEXT</term>

              <listitem>
                <para>Content to be inserted into the <tag>&lt;title&gt;</tag> section tag.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>meta=HASHREF</term>

              <listitem>
                <para>Meta section content, supplied as a hash reference. Processing is nuanced. Standard <emphasis>key</emphasis>=&gt;<emphasis>value</emphasis> hash pairs are displayed as
                &lt;meta name=key content=value&gt; meta tags. Pairs of the type <emphasis>"property=name"=&gt;value</emphasis> are displayed as <tag>&lt;meta property=name
                content=value&gt;</tag>.</para>

                <programlisting language="html">&lt;start_html meta="%{ author =&gt; 'Bob Smith', 'http-equiv=refresh' =&gt; '5; url=https://www.example.com' }"&gt;</programlisting>

                <para>Will produce:</para>

                <programlisting language="html">&lt;meta name="author" content="Bob Smith"&gt;
&lt;meta http-equiv="refresh" content="5; url=https://www.example.com" &gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>style=URL | ARRAYREF</term>

              <listitem>
                <para>Stylesheets to load. Values to this attribute will be output as <property>href</property> attributes of type <property>rel=stylesheet</property> in a
                <tag>&lt;link&gt;</tag> tag.</para>

                <programlisting language="html">&lt;start_html style="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"&gt;</programlisting>

                <para>Will produce:</para>

                <programlisting language="html">&lt;link href="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4" rel="stylesheet"&gt;</programlisting>

                <para>Array types are supported as values to the style property to allow multiple style sheet <tag>&lt;link&gt;</tag> tags to be created at once, e.g.</para>

                <programlisting language="html">&lt;start_html style="@{
    'https://cdn.jsdelivr.net/npm/water.css@2/out/water.css',
    'https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css'
}"&gt;</programlisting>

                <para>Will produce:</para>

                <programlisting language="html">&lt;link href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css" rel="stylesheet"&gt;
&lt;link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet"&gt;</programlisting>

                <para>Linked stylesheets are emitted before <property>include_style</property> content.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>style_prepend=URL | ARRAYREF</term>
              <term>style_append=URL | ARRAYREF</term>

              <listitem>
                <para>Stylesheets to add before or after the current <property>style</property> value. This is useful when
                <varname>WEBDYNE_START_HTML_PARAM</varname> supplies default styles and a page needs to add styles without replacing those defaults.</para>

                <programlisting language="html">&lt;start_html style_append="app.css"&gt;</programlisting>

                <para>The existing <property>style</property> attribute keeps its current override behaviour. If <property>style</property> is supplied with
                <property>style_prepend</property> or <property>style_append</property>, the prepend and append values are added around the explicit style value.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>script=URL | ARRAYREF</term>

              <listitem>
                <para>Similar facility to the style attribute. Any values supplied to this attribute will be output as <property>src</property> attributes to a <tag>&lt;script&gt;</tag>
                tag.</para>

                <programlisting language="html">&lt;start_html script="https://cdn.jsdelivr.net/npm/chart.js"&gt;</programlisting>

                <para>Will produce:</para>

                <programlisting language="html">&lt;script src="https://cdn.jsdelivr.net/npm/chart.js"&gt;</programlisting>

                <para>Anything supplied after the URL section as an anchor will be used in the script tag as an attribute, e.g.</para>

                <programlisting language="html">&lt;start_html script="https://cdn.jsdelivr.net/npm/alpinejs@3/dist/cdn.min.js#defer&amp;integrity=sha.."&gt;</programlisting>

                <para>Will produce:</para>

                <programlisting language="html">&lt;script defer integrity=sha.. src="https://cdn.jsdelivr.net/npm/alpinejs@3/dist/cdn.min.js"&gt;</programlisting>

                <para>As per the style attribute you can supply an array of resources to load using the same syntax.</para>

                <para>External scripts are emitted before <property>include_script</property> content.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>script_prepend=URL | ARRAYREF</term>
              <term>script_append=URL | ARRAYREF</term>

              <listitem>
                <para>Scripts to add before or after the current <property>script</property> value. This is useful when
                <varname>WEBDYNE_START_HTML_PARAM</varname> supplies default scripts and a page needs to add scripts without replacing those defaults.</para>

                <programlisting language="html">&lt;start_html script_append="app.js"&gt;</programlisting>

                <para>The existing <property>script</property> attribute keeps its current override behaviour. If <property>script</property> is supplied with
                <property>script_prepend</property> or <property>script_append</property>, the prepend and append values are added around the explicit script value.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>base, target = URL, TARGET</term>

              <listitem>
                <para>Generate a &lt;base&gt; tag within the &lt;head&gt; section containing attributes equivalent to <property>href=value</property> of the base attribute,
                <property>target=value</property> of the target attribute</para>

                <programlisting language="html">&lt;start_html base="https://example.com" target="_blank"&gt;</programlisting>

                <para>Will produce:</para>

                <programlisting language="html">&lt;base href="https://example.com/" target="_blank"&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>author=EMAIL</term>

              <listitem>
                <para>Generate an author link in the <tag>&lt;head&gt;</tag> section. The supplied value is URL encoded and emitted as a <tag>&lt;link rel="author"
                href="mailto:..."&gt;</tag> tag.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>include=PATH | ARRAYREF</term>

              <listitem>
                <para>Will include the raw text from the nominated file (supplied as the value) within the <tag>&lt;head&gt;</tag> section. No processing is done, the file contents are
                inserted verbatim.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>include_script=PATH | ARRAYREF</term>

              <listitem>
                <para>As per above <property>include</property> attribute, raw text from the nominated file is inserted into the <tag>&lt;head&gt;</tag> section, however it is wrapped in a
                <tag>&lt;script&gt;</tag> tag. Included scripts are emitted after external scripts from the <property>script</property> attribute.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>include_style=PATH | ARRAYREF</term>

              <listitem>
                <para>As per standard <property>include</property> attribute raw text from the nominated file is inserted into the <tag>&lt;head&gt;</tag> section, however it is wrapped in a
                <tag>&lt;style&gt;</tag> tag. Included styles are emitted after linked stylesheets from the <property>style</property> attribute.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>static</term>

              <listitem>
                <para>Boolean. If the static attribute is present in the <tag>&lt;start_html&gt;</tag> tag the entire page is designated static. It will be compiled and generated once (at
                first load) and the resulting HTML will be cached and served on subsequent loads.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>cache=METHOD</term>

              <listitem>
                <para>Use a cache handler to determine how often the page should be recompiled. See the <link linkend="caching_section">Caching</link> section. Sample:</para>

                <programlisting language="html">&lt;start_html cache="&amp;cache"&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>handler=METHOD</term>

              <listitem>
                <para>Store a page handler method in the compiled page metadata. This is used by WebDyne's request dispatch path when a page needs to nominate a handler from the
                <tag>&lt;start_html&gt;</tag> tag instead of relying on surrounding server configuration.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>h1 | h2 | h3 | h4 | h5 | h6</term>

              <listitem>
                <para>Boolean heading shortcut. If one of these attributes is present and a title is available, WebDyne inserts a heading of the requested level at the start of the generated
                <tag>&lt;body&gt;</tag> using the page title text.</para>

                <programlisting language="html">&lt;start_html title="Hello World" h1&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>hr</term>

              <listitem>
                <para>Boolean heading companion shortcut. When used with one of <parameter>h1</parameter> through <parameter>h6</parameter>, WebDyne inserts an <tag>&lt;hr&gt;</tag> immediately
                after the generated heading.</para>

                <programlisting language="html">&lt;start_html title="Hello World" h1 hr&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>sse=METHOD</term>

              <listitem>
                <para>Mark the page as providing Server-Sent Events when run through the PAGI request layer. The value is stored in WebDyne page metadata and names the async subroutine to call
                for SSE requests. If used as a bare boolean attribute, the method name defaults to <function>sse</function>.</para>

                <programlisting language="html">&lt;start_html sse&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>ws=METHOD</term>

              <listitem>
                <para>Mark the page as providing WebSocket handling when run through the PAGI request layer. The value is stored in WebDyne page metadata and names the async subroutine to call
                for WebSocket requests. If used as a bare boolean attribute, the method name defaults to <function>ws</function>.</para>

                <programlisting language="html">&lt;start_html ws&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>pico</term>

              <listitem>
                <para>Framework shortcut defined by <varname>WEBDYNE_START_HTML_SHORTCUT_HR</varname>. Adds Pico CSS to the <property>style</property> list, using
                <literal>https://cdn.jsdelivr.net/npm/@picocss/pico@latest/css/pico.min.css</literal> by default.</para>

                <programlisting language="html">&lt;start_html pico title="Pico Example"&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>htmx</term>

              <listitem>
                <para>Framework shortcut defined by <varname>WEBDYNE_START_HTML_SHORTCUT_HR</varname>. Adds htmx to the <property>script</property> list, using
                <literal>https://cdn.jsdelivr.net/npm/htmx.org@latest/dist/htmx.min.js</literal> by default.</para>

                <programlisting language="html">&lt;start_html htmx&gt;</programlisting>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>alpine</term>

              <listitem>
                <para>Framework shortcut defined by <varname>WEBDYNE_START_HTML_SHORTCUT_HR</varname>. Adds Alpine.js to the <property>script</property> list, using
                <literal>https://cdn.jsdelivr.net/npm/alpinejs@latest/dist/cdn.min.js#defer</literal> by default. The URL fragment is interpreted as script attributes, so the generated
                <tag>&lt;script&gt;</tag> tag includes <property>defer</property>.</para>

                <programlisting language="html">&lt;start_html alpine&gt;</programlisting>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_end_html">
          <info>
            <title><tag>&lt;end_html&gt;</tag></title>
          </info>

          <para>End a HTML page. This will produce the <tag>&lt;/body&gt;&lt;/html&gt;</tag> tags. It is not strictly necessary as the parser will automatically close dangling tags if it gets
          to the end of the file without seeing them, however is provided for completeness.</para>
        </section>
      </section>

      <section xml:id="tag_reference_forms">
        <info>
          <title>Form Tags</title>
        </info>

        <section xml:id="tag_popup_menu">
          <info>
            <title><tag>&lt;popup_menu&gt;</tag></title>
          </info>

          <para>Provide a drop-down menu of options for a user to select from. Other standard HTML <tag>&lt;select&gt;</tag> attributes are passed through to the generated element.</para>

          <programlisting language="html">&lt;popup_menu
  name=NAME
  values=ARRAYREF | HASHREF
  [labels=HASHREF]
  [attributes=HASHREF]
  [label=TEXT]
  [multiple]
  [disabled=VALUE | ARRAYREF]
  [selected | defaults | default=VALUE | ARRAYREF]
  [force]
  (standard HTML &lt;select&gt; attributes)
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>name=NAME</term>

              <listitem>
                <para>The name associated with this form component. This is the CGI parameter to be interrogated for value(s) once the form is submitted.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>values=ARRAYREF | HASHREF</term>

              <listitem>
                <para>List of items to be presented in the drop down. If an array ref labels will be the same as values. If a hash ref the value will be the hash key, the label the hash
                label. If predictable display order matters, use an array ref for <property>values</property> and a separate <property>labels</property> hash ref.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>labels=HASHREF</term>

              <listitem>
                <para>If values is presented as an array ref, a hash ref of labels to be associated with each value can be supplied.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>attributes=HASHREF</term>

              <listitem>
                <para>Hash reference of additional attributes to apply to individual <tag>&lt;option&gt;</tag> elements, keyed by option value.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>label=TEXT</term>

              <listitem>
                <para>Wrap the generated <tag>&lt;select&gt;</tag> element in a <tag>&lt;label&gt;</tag> element using the supplied text.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>multiple</term>

              <listitem>
                <para>Boolean. If enabled allows multiple options to be selected. If not present (disabled) only one option can be selected.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>disabled=VALUE | ARRAYREF</term>

              <listitem>
                <para>Single (string scalar) item or multiple (array ref) items which should be greyed out (not selectable) in menu options.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>selected | defaults | default=VALUE | ARRAYREF</term>

              <listitem>
                <para>Single (string scalar) item or multiple (array ref) items which should be pre-selected in menu options.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>force</term>

              <listitem>
                <para>By default selected values are stateful, and submitted values for this field override the configured default or selected values. Setting <property>force</property> always
                uses the configured default or selected values.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_radio_group">
          <info>
            <title><tag>&lt;radio_group&gt;</tag></title>
          </info>

          <para>Provide a grouped list of radio buttons for a user to choose from. Only one radio button item can be selected.</para>

          <programlisting language="html">&lt;radio_group
  name=NAME
  values=ARRAYREF | HASHREF
  [labels=HASHREF]
  [attributes=HASHREF]
  [disabled=VALUE | ARRAYREF]
  [checked | defaults | default=VALUE | ARRAYREF]
  [linebreak]
  [force]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>name=NAME</term>

              <listitem>
                <para>The name associated with this form component. This is the CGI parameter to be interrogated for value(s) once the form is submitted.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>values=ARRAYREF | HASHREF</term>

              <listitem>
                <para>List of items to be presented. If an array reference, labels will be the same as values. If a hash reference the value will be the hash key, the label the hash value. If
                predictable display order matters, use an array reference for <property>values</property> and a separate <property>labels</property> hash reference.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>labels=HASHREF</term>

              <listitem>
                <para>If values is presented as an array ref a hash ref of labels to be associated with each value can be supplied.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>attributes=HASHREF</term>

              <listitem>
                <para>Hash reference of additional attributes to apply to individual <tag>&lt;input type="radio"&gt;</tag> elements, keyed by radio value.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>disabled=VALUE | ARRAYREF</term>

              <listitem>
                <para>Single (string) or multiple (array ref) of items which should be greyed out (not selectable) in radio group items.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>checked | defaults | default=VALUE | ARRAYREF</term>

              <listitem>
                <para>Single (string) or multiple (array ref) items which should be pre-selected in radio group items. Radio groups can only select one value; if multiple defaults are supplied,
                the first value in sorted order is used.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>linebreak</term>

              <listitem>
                <para>Separate generated radio items with <tag>&lt;br&gt;</tag> tags.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>force</term>

              <listitem>
                <para>By default checked values are stateful, and submitted values for this field override the configured defaults. Setting <property>force</property> always uses the
                configured defaults.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_checkbox_group">
          <info>
            <title><tag>&lt;checkbox_group&gt;</tag></title>
          </info>

          <para>Provide a grouped list of checkbox items for a user to choose from. Multiple checkboxes can be selected.</para>

          <programlisting language="html">&lt;checkbox_group
  name=NAME
  values=ARRAYREF | HASHREF
  [labels=HASHREF]
  [attributes=HASHREF]
  [disabled=VALUE | ARRAYREF]
  [checked | defaults | default=VALUE | ARRAYREF]
  [linebreak]
  [force]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>name=NAME</term>

              <listitem>
                <para>The name associated with this form component. This is the <code>$self-&gt;CGI()</code> parameter to be interrogated for value(s) once the form is submitted.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>values=ARRAYREF | HASHREF</term>

              <listitem>
                <para>List of items to be presented. If an array ref labels will be the same as values. If a hash ref the value will be the hash key, the label the hash label. If predictable
                display order matters, use an array ref for <property>values</property> and a separate <property>labels</property> hash ref.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>labels=HASHREF</term>

              <listitem>
                <para>If values is presented as an array ref a hash ref of labels to be associated with each value can be supplied.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>attributes=HASHREF</term>

              <listitem>
                <para>Hash reference of additional attributes to apply to individual <tag>&lt;input type="checkbox"&gt;</tag> elements, keyed by checkbox value.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>disabled=VALUE | ARRAYREF</term>

              <listitem>
                <para>Single (string) or multiple (array ref) items which should be greyed out (not selectable) in checkbox group items.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>checked | defaults | default=VALUE | ARRAYREF</term>

              <listitem>
                <para>Single (scalar) or multiple (array ref) items which should be pre-selected in checkbox group items.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>linebreak</term>

              <listitem>
                <para>Separate generated checkbox items with <tag>&lt;br&gt;</tag> tags.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>force</term>

              <listitem>
                <para>By default checked values are stateful, and submitted values for this field override the configured defaults. Setting <property>force</property> always uses the
                configured defaults.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_checkbox">
          <info>
            <title><tag>&lt;checkbox&gt;</tag></title>
          </info>

          <para>Single checkbox for a user to select or clear.</para>

          <programlisting language="html">&lt;checkbox
  name=NAME
  [value=VALUE | BOOLEAN]
  [checked]
  [disabled]
  [label=TEXT]
  (standard HTML &lt;input&gt; attributes)
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>name=NAME</term>

              <listitem>
                <para>The name associated with this form component. This is the CGI parameter to be interrogated for value(s) once the form is submitted.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>value=VALUE | BOOLEAN</term>

              <listitem>
                <para>The value to be returned in the CGI parameter if this checkbox is ticked (selected). If not supplied defaults to 1.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>disabled</term>

              <listitem>
                <para>If present the checkbox will be displayed but cannot be selected.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>checked</term>

              <listitem>
                <para>If present the checkbox is initially selected. For the default boolean value path, submitted values for the field are stateful and override this initial setting.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>label=TEXT</term>

              <listitem>
                <para>Wrap the generated checkbox in a <tag>&lt;label&gt;</tag> element using the supplied text.</para>
              </listitem>
            </varlistentry>
          </variablelist>

          <note>
            <para>In order to retain state all checkbox form items will present a hidden parameter with the same name as the checkbox. This is notable because querying the parameter associated
            with a checkbox component will always return a <classname>Hash::MultiValue</classname> object with two items, the last of which is the checkbox value. When using
            <code>$self-&gt;CGI-&gt;param(&lt;checkbox name&gt;)</code> form of query, or <code>$_{&lt;checkbox name&gt;}</code> the user selected checkbox value will always be returned as a
            boolean or scalar value. This automatic hidden-field and persistence behaviour applies when no explicit <property>value</property> is supplied.</para>
          </note>
        </section>

        <section xml:id="tag_scrolling_list">
          <info>
            <title><tag>&lt;scrolling_list&gt;</tag></title>
          </info>

          <para>Presents a scrolling list of options for a user to choose from. It uses the same attributes as <tag>&lt;popup_menu&gt;</tag> with the addition of a
          <property>size</property> attribute.</para>

          <programlisting language="html">&lt;scrolling_list
  (same attributes as &lt;popup_menu&gt;)
  [size=ROWS]
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>size=ROWS</term>

              <listitem>
                <para>Number of rows to make visible in the user interface for the scrolling list. If omitted, WebDyne sets this to the number of configured values.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_textarea">
          <info>
            <title><tag>&lt;textarea&gt;</tag></title>
          </info>

          <para>A text box for freeform text entry. All attributes are the same as the HTML standard <tag>&lt;textarea&gt;</tag> tag with attributes:</para>

          <programlisting language="html">&lt;textarea
  name=NAME
  [default=TEXT]
  [label=TEXT]
  [force]
  (all standard HTML &lt;textarea&gt; attributes)
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>name=NAME</term>

              <listitem>
                <para>The name associated with this form component. This is the CGI parameter to be interrogated for value(s) once the form is submitted.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>default=TEXT</term>

              <listitem>
                <para>The default content to be pre-filled out in the <tag>&lt;textarea&gt;</tag> component</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>label=TEXT</term>

              <listitem>
                <para>Wrap the generated <tag>&lt;textarea&gt;</tag> element in a <tag>&lt;label&gt;</tag> element using the supplied text.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>force</term>

              <listitem>
                <para>By default the component is stateful, and user entered text will persist after form submission. Setting the <property>force</property> attribute will always present the
                default content regardless of user input.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_textfield">
          <info>
            <title><tag>&lt;textfield&gt;</tag></title>
          </info>

          <para>The standard <tag>&lt;input type="text"&gt;</tag> tag type. User input with this tag is persistent. Other standard HTML <tag>&lt;input&gt;</tag> attributes are passed through to
          the generated element.</para>

          <programlisting language="html">&lt;textfield
  name=NAME
  [value=TEXT]
  [label=TEXT]
  [force]
  (standard HTML &lt;input&gt; attributes)
&gt;</programlisting>

          <variablelist>
            <varlistentry>
              <term>label=TEXT</term>

              <listitem>
                <para>Wrap the generated input in a <tag>&lt;label&gt;</tag> element using the supplied text.</para>
              </listitem>
            </varlistentry>

            <varlistentry>
              <term>force</term>

              <listitem>
                <para>By default the field is stateful, and submitted values override the configured value. Setting <property>force</property> always uses the configured value.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>

        <section xml:id="tag_password_field">
          <info>
            <title><tag>&lt;password_field&gt;</tag></title>
          </info>

          <para>The standard <tag>&lt;input type="password"&gt;</tag> tag type. It uses the same attributes as <tag>&lt;textfield&gt;</tag>, with the generated input type set to
          <literal>password</literal>.</para>
        </section>

        <section xml:id="tag_filefield">
          <info>
            <title><tag>&lt;filefield&gt;</tag></title>
          </info>

          <para>The standard <tag>&lt;input type="file"&gt;</tag> tag type. It uses the same attributes as <tag>&lt;textfield&gt;</tag>, with the generated input type set to
          <literal>file</literal>. When querying this parameter after form submission responses will be in the form of a Plack::Request::Upload object. Example to demonstrate minimal file
          upload facility:</para>

          <programlisting language="html">&lt;start_html title="File Upload"&gt;
&lt;start_multipart_form&gt;
&lt;filefield name="file" multiple required&gt;
&lt;p&gt;
&lt;submit name=Upload&gt;
&lt;end_form&gt;

&lt;pre&gt;
&lt;perl handler/&gt;
&lt;/pre&gt;

__PERL__

use Data::Dumper;
sub handler {

    my $self=shift();
    my $cgi_or=$self-&gt;CGI();
    return Dumper($cgi_or-&gt;uploads()-&gt;flatten);

}</programlisting>
        </section>

        <section xml:id="tag_image_button">
          <info>
            <title><tag>&lt;image_button&gt;</tag></title>
          </info>

          <para>The standard <tag>&lt;input type="image"&gt;</tag> tag type. It accepts standard HTML <tag>&lt;input&gt;</tag> attributes and supports <property>label</property> wrapping using
          the same behaviour as <tag>&lt;textfield&gt;</tag>.</para>
        </section>

        <section xml:id="tag_button">
          <info>
            <title><tag>&lt;button&gt;</tag></title>
          </info>

          <para>The standard HTML <tag>&lt;button&gt;</tag> element. Standard button attributes and enclosed content are passed through to the generated element.</para>
        </section>

        <section xml:id="tag_submit">
          <info>
            <title><tag>&lt;submit&gt;</tag></title>
          </info>

          <para>The standard <tag>&lt;input type="submit"&gt;</tag> tag type to initiate form submission. It accepts standard HTML <tag>&lt;input&gt;</tag> attributes and supports
          <property>label</property> wrapping using the same behaviour as <tag>&lt;textfield&gt;</tag>.</para>
        </section>

        <section xml:id="tag_reset">
          <info>
            <title><tag>&lt;reset&gt;</tag></title>
          </info>

          <para>The standard <tag>&lt;input type="reset"&gt;</tag> tag type to reset form fields to their initial values. It accepts standard HTML <tag>&lt;input&gt;</tag> attributes and
          supports <property>label</property> wrapping using the same behaviour as <tag>&lt;textfield&gt;</tag>.</para>
        </section>

        <section xml:id="tag_defaults">
          <info>
            <title><tag>&lt;defaults&gt;</tag></title>
          </info>

          <para>A CGI-style shortcut rendered as a submit input. It is supported for compatibility with CGI.pm-style form helpers and uses the same attributes as <tag>&lt;submit&gt;</tag>.</para>
        </section>

        <section xml:id="tag_hidden">
          <info>
            <title><tag>&lt;hidden&gt;</tag></title>
          </info>

          <para>The standard <tag>&lt;input type="hidden"&gt;</tag> tag type. Standard HTML <tag>&lt;input&gt;</tag> attributes are passed through to the generated element.</para>
        </section>

        <section xml:id="tag_isindex">
          <info>
            <title><tag>&lt;isindex&gt;</tag></title>
          </info>

          <para>Legacy <tag>&lt;isindex&gt;</tag> output is supported for compatibility, but the HTML element is deprecated and should not be used for new pages. Prefer normal form tags such
          as <tag>&lt;start_form&gt;</tag>, <tag>&lt;textfield&gt;</tag>, and <tag>&lt;submit&gt;</tag>.</para>
        </section>

        <section xml:id="tag_start_form">
          <info>
            <title><tag>&lt;start_form&gt;</tag></title>
          </info>

          <para>Start a form with <property>method=POST</property>. Other standard HTML <tag>&lt;form&gt;</tag> attributes are passed through to the generated element.</para>

          <programlisting language="html">&lt;start_form
  [method=METHOD]
  (standard HTML &lt;form&gt; attributes)
&gt;</programlisting>
        </section>

        <section xml:id="tag_start_multipart_form">
          <info>
            <title><tag>&lt;start_multipart_form&gt;</tag></title>
          </info>

          <para>Start a form with <property>method=POST</property> and <property>enctype="multipart/form-data"</property>. Other standard HTML <tag>&lt;form&gt;</tag> attributes are passed
          through to the generated element. The <property>enctype</property> attribute can be overridden because form attributes are passed through, but this is normally only useful if you are
          deliberately replacing multipart form encoding.</para>

          <programlisting language="html">&lt;start_multipart_form
  [method=METHOD]
  [enctype=ENCODING]
  (standard HTML &lt;form&gt; attributes)
&gt;</programlisting>
        </section>
      </section>
    </section>

    <section xml:id="method_reference">
      <info>
        <title>Method Reference</title>
      </info>

      <para>When running Perl code within a WebDyne page the very first parameter passed to any routine (in-line or in a __PERL__ block) is an instance of the WebDyne page object (referred to
      as <varname>$self</varname> in most of the examples, e.g. <code>$self-&gt;print("Hello World")</code>). All methods return <returnvalue>undef</returnvalue> on failure, and raise an error
      using the <function>err()</function> function. The following methods are available to any instance of the WebDyne object:</para>

      <variablelist>
        <varlistentry>
          <term><methodname>CGI()</methodname></term>

          <listitem>
            <para>Returns an instance of a CGI::Simple type object for the current request.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>r(), request()</methodname></term>

          <listitem>
            <para>Returns the current WebDyne request adapter. WebDyne provides a common request interface across Apache, PSGI, PAGI and command-line render contexts, with backend-specific
            details available where supported.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>html_tiny()</methodname></term>

          <listitem>
            <para>Returns an instance of the HTML::Tiny object, can be used for creating programmatic HTML output</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>include()</methodname></term>

          <listitem>
            <para>Returns HTML derived from a file, using the same parameters as the <tag>&lt;include&gt;</tag> tag</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>render( <replaceable>key=&gt;value, key=&gt;value</replaceable>, .. )</methodname></term>

          <listitem>
            <para>Called to render the text or HTML between <tag>&lt;perl&gt;..&lt;/perl&gt;</tag> tags. Optional key and value pairs will be substituted into the output as per the variable
            section. Returns a scalar ref of the resulting HTML.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>render_block( blockname, <replaceable>key=&gt;value, key=&gt;valufge, ..</replaceable>).</methodname></term>

          <listitem>
            <para>Called to render a block of text or HTML between &lt;block&gt;..&lt;/block&gt; tags. Optional key and value pairs will be substituted into the output as per the variable
            section. Returns scalar ref of resulting HTML if called with from &lt;perl&gt;..&lt;/perl&gt; section containing the block to be rendered, or true (<code>\undef</code>) if the
            block is not within the &lt;perl&gt;..&lt;/perl&gt; section (e.g. further into the document, see the block section for an example). Rendered blocks must be "published' if
            visibility required via return as array, or return of <code>$self-&gt;render()</code>.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>render_reset()</methodname></term>

          <listitem>
            <para>Erase anything previously set to render - it will not be sent to the browser. Limited use, may be helpful in error handling to "pull" anything previously published and
            replace with error message.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>redirect( uri=&gt;uri | file=&gt;filename | html=&gt;\html_text | json=&gt;\json_text | text=&gt;\plain_text)</methodname></term>

          <listitem>
            <para>Will redirect to URI or file nominated, or display only nominated text. Any rendering done to prior to this method is abandoned. If supplying HTML text to be rendered supply
            as a SCALAR reference. Content type header will be automatically adjusted to MIME type appropriate for type if redirecting to html, json or plain text content.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>inode( <replaceable>seed</replaceable>, <replaceable>seed</replaceable> )</methodname></term>

          <listitem>
            <para>Returns the page unique ID (UID). Called inode for legacy reasons, as that is what the UID used to be based on. If a seed value is supplied a new UID will be generated based
            on an MD5 of the seed(s) combined with other information (such as <code>$r-&gt;location</code>) to generate a unique UUID. Seed only needs to be supplied if using cache handlers,
            see the "<link linkend="caching_section">Caching</link>" section</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>cache_mtime( <replaceable>uid</replaceable> )</methodname></term>

          <listitem>
            <para>Returns the mtime (modification time) of the cache file associated with the optionally supplied UID. If no UID supplied the current one will be used. Can be used to make
            cache compile decisions by WebDyne::Cache code (e.g if page &gt; x minutes old, recompile).</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>source_mtime()</methodname></term>

          <listitem>
            <para>Returns the mtime (modification time) of the source PSP file currently being rendered.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>cache_compile()</methodname></term>

          <listitem>
            <para>Force recompilation of cache file. Can be used in cache code to force recompilation of a page, even if it is flagged static. Returns current value if no parameters supplied,
            or sets if parameter supplied.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>filename()</methodname></term>

          <listitem>
            <para>Return the full filename (including path) of the file being rendered. Will only return the core (main) filename - any included files, templates etc. are not reported.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>cwd()</methodname></term>

          <listitem>
            <para>Return the current working directory WebDyne is operating in.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>no_cache()</methodname></term>

          <listitem>
            <para>Send headers indicating that the page is not be cached by the browser or intermediate proxies. By default WebDyne pages automatically set the no-cache headers, although this
            behaviour can be modified by clearing the <varname>$WEBDYNE_NO_CACHE</varname> variable and using this function</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>meta()</methodname></term>

          <listitem>
            <para>Return a hash ref containing the meta data for this page. Alterations to meta data are persistent for this process, and carry across Apache requests (although not across
            different Apache processes)</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>print( <replaceable>output</replaceable> ), printf( <replaceable>output</replaceable> ), say( <replaceable>output</replaceable> )</methodname></term>

          <listitem>
            <para>Render the output of the <methodname>print()</methodname>, <methodname>printf()</methodname> or <methodname>say()</methodname> routines into the current HTML stream. The
            <methodname>print()</methodname> and <methodname>printf()</methodname> methods emulate their Perl functions in not appending a new line into the output (unless autonewline() is
            set), where as <methodname>say()</methodname> does.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>autonewline()</term>

          <listitem>
            <para>Get or set the autonewline flag. If set will add a new line automatically when using <methodname>print()</methodname> or <code>$self-&gt;print()</code>, essentially emulating
            say(). Supply undef or 0 to clear.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><methodname>render_time()</methodname></term>

          <listitem>
            <para>Return the elapsed time since the WebDyne hander started rendering this page. Obviously only meaningful if called at the end of a page, just before final output to
            browser.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>err( <replaceable>message</replaceable> )</term>

          <listitem>
            <para>Return and/or raise an error to the WebDyne handler. Supply the actual error message as text.</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </section>

    <section xml:id="configuration_reference">
      <title>Configuration Reference</title>

      <section xml:id="webdyne_constants">
        <info>
          <title>Constants</title>
        </info>

        <para>Constants defined in the WebDyne::Constant package control various aspects of how WebDyne behaves. Constants can be modified globally by altering a global configuration file
        (<filename>/etc/webdyne.conf.pl</filename> under Linux distros), setting environment variable or by altering configuration parameters within the Apache web server config.</para>

        <section>
          <info>
            <title>Global constants file</title>
          </info>

          <para>WebDyne will look for a system constants file under <filename>/etc/webdyne.conf.pl</filename> and set package variables according to values found in that file. The file is in
          Perl Data::Dumper format, and takes the format:</para>

          <programlisting># sample /etc/webdyne.conf.pl file
#
$VAR1={
        WebDyne::Constant =&gt; {

                WEBDYNE_CACHE_DN       =&gt; '/data1/webdyne/cache',
                WEBDYNE_STORE_COMMENTS =&gt; 1,
                #  ... more variables for WebDyne package

       },

       WebDyne::Session::Constant =&gt; {

                WEBDYNE_SESSION_ID_COOKIE_NAME =&gt; 'session_cookie',
                #  ... more variables for WebDyne::Session package

       },

};</programlisting>

          <para>The file is not present by default and should be created if you wish to change any of the WebDyne constants from their default values.</para>

          <important>
            <para>Always check the syntax of the <filename>/etc/webdyne.conf.pl</filename> file after editing by running <command>perl -c -w /etc/webdyne.conf.pl</command> to check that the
            file is readable by Perl. Files with syntax errors will fail silently and the variables will revert to module defaults.</para>
          </important>
        </section>

        <section>
          <info>
            <title>PSGI and PAGI root configuration</title>
          </info>

          <para>When the <command>webdyne.psgi</command> or <command>webdyne.pagi</command> wrapper builds an application it also loads
          <filename>$DOCUMENT_ROOT/.webdyne.conf.pl</filename>, if present. This applies both when the wrapper is started directly and when it is loaded by an external PSGI or PAGI server. If
          the effective document root is a file, the parent directory is checked for <filename>.webdyne.conf.pl</filename>.</para>

          <para>This root configuration file is separate from request-directory configuration. When <varname>WEBDYNE_DIR_CONFIG_CWD_LOAD</varname> is enabled, WebDyne can also inspect a
          <filename>.webdyne.conf.pl</filename> file in the directory of the current PSP file, but only the <varname>WEBDYNE_DIR_CONFIG</varname> section is used from that per-directory
          file.</para>
        </section>

        <section>
          <info>
            <title>Setting WebDyne constants in Apache</title>
          </info>

          <para>WebDyne constants can be set in an Apache httpd.conf file using the PerlSetVar directive:</para>

          <para><programlisting>
PerlHandler     WebDyne
PerlSetVar      WEBDYNE_CACHE_DN                '/data1/webdyne/cache'
PerlSetVar      WEBDYNE_STORE_COMMENTS          1

#  From WebDyne::Session package
#
PerlSetVar      WEBDYNE_SESSION_ID_COOKIE_NAME  'session_cookie'

</programlisting></para>

          <para><important>
              <para>WebDyne constants cannot be set on a per-location or per-directory basis - they are read from the top level of the config file and set globally.</para>

              <para>Some 1.x versions of mod_perl do not read PerlSetVar variables correctly. If you encounter this problem use a <tag>&lt;Perl&gt;..&lt;/Perl&gt;</tag> section in the
              <filename>httpd.conf</filename> file, e.g.:</para>

              <programlisting># Mod_perl 1.x

PerlHandler     WebDyne
&lt;Perl&gt;
$WebDyne::Constant::WEBDYNE_CACHE_DN='/data1/webdyne/cache';
$WebDyne::Constant::WEBDYNE_STORE_COMMENTS=1;
$WebDyne::Session::Constant::WEBDYNE_SESSION_ID_COOKIE_NAME='session_cookie';
&lt;/Perl&gt;

</programlisting>
            </important></para>

          <para>Where you need to set variables without simple string content you can use a <tag>&lt;Perl&gt;..&lt;/Perl&gt;</tag> section in the <filename>httpd.conf</filename> file,
          e.g.:</para>

          <programlisting># Setting more complex variables

PerlHandler     WebDyne
&lt;Perl&gt;
$WebDyne::Constant::WEBDYNE_CACHE_DN='/data1/webdyne/cache';
$WebDyne::Constant::WEBDYNE_STORE_COMMENTS=1;
$WebDyne::Session::Constant::WEBDYNE_SESSION_ID_COOKIE_NAME='session_cookie';
&lt;/Perl&gt;

</programlisting>

          <warning>
            <para>The letsencrypt <command>certbot</command> utility will error out when trying to update any Apache config file with <code>&lt;Perl&gt;</code> sections. To avoid this you put
            the variables in a separate file and include them, e.g. in the <filename>apache.conf</filename> file:</para>

            <para><programlisting># Some config setting defaults. See documentation for full range.
# Commented out # options represent defaults
#
PerlRequire conf.d/webdyne_constant.pl</programlisting></para>

            <para>And then in the webdyne_constant.pl file:</para>

            <para><programlisting>use WebDyne;
use WebDyne::Constant;

#  Error display/extended display on/off. More granular options below.
#  Set to 1 to enable, 0 to disable
#
$WebDyne::WEBDYNE_ERROR_SHOW=1;
$WebDyne::WEBDYNE_ERROR_SHOW_EXTENDED=1;

#  Extended error control.
#
#  $WebDyne::WEBDYNE_ERROR_SOURCE_CONTEXT_SHOW=1;
#  $WebDyne::WEBDYNE_ERROR_SOURCE_CONTEXT_LINES_PRE=4;
#  $WebDyne::WEBDYNE_ERROR_SOURCE_CONTEXT_LINES_POST=4;
</programlisting></para>
          </warning>
        </section>

        <section>
          <info>
            <title>Constants Reference</title>
          </info>

          <para>The authoritative reference for constants defined by <classname>WebDyne::Constant</classname> is maintained with the module sidecar documentation and copied into the MkDocs
          module reference during documentation generation.</para>

          <para>See <link role="manual-page-new-tab" xlink:href="modules/WebDyne_Constant.md" xmlns:xlink="http://www.w3.org/1999/xlink"><classname>WebDyne::Constant</classname> module
          reference</link> for the complete list of constants, defaults, and behaviour descriptions.</para>

          <tip>
            <para>Configuration items can be overridden by setting environment variables of the same name with the desired value.</para>
          </tip>

          <para>Extension modules, for example <classname>WebDyne::Session</classname>, may define their own constants in their own <classname>WebDyne::*::Constant</classname> packages. See
          the relevant module reference pages for details.</para>
        </section>
      </section>

      <section xml:id="environment_variable_reference">
        <title>Environment Variable Reference</title>

        <para>All WebDyne configuration items can be overridden by setting an environment variable of the same name when starting PSGI or PAGI instances, or via an Apache SetEnv directive,
        e.g:</para>

        <screen>#  Start webdyne plack instance with extended error display for this run.
#
$ WEBDYNE_ERROR_SHOW_EXTENDED=1 plackup `which webdyne.psgi`</screen>

        <para>In addition to the configuration overrides the following environment variables are available:</para>

        <section xml:id="env_webdyne_conf">
          <title>WEBDYNE_CONF</title>

          <para>Location of the WebDyne configuration file to load. Loading of an alternate configuration file will bypass loading of any/all other configuration files (e.g.
          /etc/webdyne.conf.pl). They are not additive - only configuration directives in the nominated by this environment variable will be processed. e.g.</para>

          <screen>#  Start webdyne with an alternate config file
#
WEBDYNE_CONF=./myconf.pl webdyne.psgi</screen>

          <caution>
            <para>Your config file must be valid Perl syntax and in the format expected. Always check it with <command>perl -c -w myconf.pl</command> to ensure it is correct.</para>
          </caution>
        </section>

        <section xml:id="env_document_root">
          <title>DOCUMENT_ROOT</title>

          <para>The starting home directory or file name (if file rather than directory) for PSGI and PAGI server wrappers to use. Defaults to the current working directory if none
          specified.</para>
        </section>

        <section xml:id="env_document_default">
          <title>DOCUMENT_DEFAULT</title>

          <para>The default file to look for in a directory if none is given via browser URL. The PSGI and PAGI constant layer defaults this to <filename>app.psp</filename>. The direct
          <command>webdyne.psgi</command> and <command>webdyne.pagi</command> command wrappers enable WebDyne's built-in index page by default unless <envar>DOCUMENT_DEFAULT</envar>,
          <option>--index=FILE</option>, or <option>--no-index</option> changes that behavior.</para>
        </section>

        <section xml:id="env_webdyne_debug">
          <title>WEBDYNE_DEBUG</title>

          <para>When debugging enabled in modules only (see Troubleshooting). Set to 1 to enable all debugging (extremely verbose), or set to module/subroutine name to filter down to that
          area. e.g.</para>

          <programlisting language="text">#  Debug the internal perl routine in WebDyne
#
$ WEBDYNE_DEBUG=perl perl -Ilib bin/wdrender time.psp
[23:28:24.699358 WebDyne (perl)] WebDyne=HASH(0x561d4a271a28) rendering perl tag in block ARRAY(0x561d4aaace20), attr $VAR1 = {
  'inline' =&gt; 1,
  'perl' =&gt; ' localtime() '
};

[23:28:24.699490 WebDyne (perl)] found inline perl code $VAR1 = \' localtime() ';
, param $VAR2 = undef;

&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt;&lt;title&gt;Untitled Document&lt;/title&gt;&lt;meta charset="UTF-8"&gt;&lt;meta content="width=device-width, initial-scale=1.0" name="viewport"&gt;&lt;link href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.classless.min.css" rel="stylesheet"&gt;&lt;link href="/style.css" rel="stylesheet"&gt;&lt;/head&gt;
&lt;body&gt;&lt;h1&gt;Example File&lt;/h1&gt;&lt;p&gt; The current server time is: Sun Jan 11 23:28:24 2026&lt;/p&gt;&lt;/body&gt;&lt;/html&gt;

</programlisting>
        </section>

        <section xml:id="env_webdyne_debug_filter">
          <title>WEBDYNE_DEBUG_FILTER</title>

          <para>When debugging enabled only output debug information that matches the regex given by this environment variable. Useful to further filter down to areas of interest.</para>
        </section>
      </section>

      <section xml:id="docker_runtime_server_tuning">
        <title>Docker Runtime Server Tuning</title>

        <para>The published WebDyne Docker images expose server tuning options through environment variables understood by the default container entrypoint.</para>

        <para>Runtime server tuning options can also be supplied through environment variables. If these variables are not set, the underlying server defaults are used.</para>

        <variablelist>
          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PSGI_WORKERS</envar></term>

            <listitem>
              <para>Sets the <command>starman</command> <option>--workers</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PSGI_MAX_REQUESTS</envar></term>

            <listitem>
              <para>Sets the <command>starman</command> <option>--max-requests</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PSGI_BACKLOG</envar></term>

            <listitem>
              <para>Sets the <command>starman</command> <option>--backlog</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PSGI_KEEPALIVE_TIMEOUT</envar></term>

            <listitem>
              <para>Sets the <command>starman</command> <option>--keepalive-timeout</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PSGI_READ_TIMEOUT</envar></term>

            <listitem>
              <para>Sets the <command>starman</command> <option>--read-timeout</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PAGI_WORKERS</envar></term>

            <listitem>
              <para>Sets the <command>pagi-server</command> <option>--workers</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PAGI_MAX_REQUESTS</envar></term>

            <listitem>
              <para>Sets the <command>pagi-server</command> <option>--max-requests</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PAGI_LISTENER_BACKLOG</envar></term>

            <listitem>
              <para>Sets the <command>pagi-server</command> <option>--listener-backlog</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PAGI_TIMEOUT</envar></term>

            <listitem>
              <para>Sets the <command>pagi-server</command> <option>--timeout</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PAGI_REQUEST_TIMEOUT</envar></term>

            <listitem>
              <para>Sets the <command>pagi-server</command> <option>--request-timeout</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PAGI_MAX_CONNECTIONS</envar></term>

            <listitem>
              <para>Sets the <command>pagi-server</command> <option>--max-connections</option> option.</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><envar>WEBDYNE_SERVER_PAGI_MAX_BODY_SIZE</envar></term>

            <listitem>
              <para>Sets the <command>pagi-server</command> <option>--max-body-size</option> option.</para>
            </listitem>
          </varlistentry>
        </variablelist>
      </section>

      <section xml:id="webdyne_directives">
        <info>
          <title>Directives</title>
        </info>

        <para>A limited number of directives are are available which change the way WebDyne processes pages. Directives are set in either the Apache .conf files and can be set differently per
        location. At this stage only one directive applies to the core WebDyne module:</para>

        <variablelist>
          <varlistentry>
            <term><varname>WebDyneHandler</varname></term>

            <listitem>
              <para>The name of the handler that WebDyne should invoke instead of handling the page internally. The only other handler available today is WebDyne::Chain.</para>
            </listitem>
          </varlistentry>
        </variablelist>

        <para>This directive exists primarily to allow PSGI to invoke WebDyne::Chain as the primary handler. It can be used in Apache httpd.conf files, but is not very efficient:</para>

        <para><programlisting>
#  This will work, but is not very efficient
#
&lt;location /shop/&gt;
PerlHandler     WebDyne
PerlSetVar      WebDyneHandler               'WebDyne::Chain'
PerlSetVar      WebDyneChain                 'WebDyne::Session'
&lt;/location&gt;


#  This is the same, and is more efficient
#
&lt;location /shop/&gt;
PerlHandler     WebDyne::Chain
PerlSetVar      WebDyneChain                 'WebDyne::Session'
&lt;/location&gt;


</programlisting></para>
      </section>
    </section>

    <section xml:id="file_locations">
      <info>
        <title>File Locations</title>
      </info>

      <variablelist>
        <varlistentry>
          <term><filename>/etc/webdyne.conf.pl, ~/.webdyne.conf.pl, $DOCUMENT_ROOT/.webdyne.conf.pl</filename></term>

          <listitem>
            <para>Used for storage of local constants that override WebDyne defaults. See the <link linkend="webdyne_constants">WebDyne::Constant</link> section for details</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </section>
  </section>

  <section xml:id="extending_webdyne">
    <info>
      <title>Extending WebDyne</title>
    </info>

    <para>WebDyne can be extended by the installation and use of supplementary Perl packages. There are several standard packages that come with the Webdyne distribution, or you can build your
    own using one of the standard packages as a template.</para>

    <para>The following gives an overview of the standard packages included in the distribution, or downloadable as extensions from CPAN.</para>

    <section>
      <info>
        <title xml:id="webdyne_chain">WebDyne::Chain</title>
      </info>

      <para>WebDyne::Chain is a module that will cascade a WebDyne request through one or more modules before delivery to the WebDyne engine. Most modules that extend WebDyne rely on
      WebDyne::Chain to get themselves inserted into the request lifecycle.</para>

      <para>Whilst WebDyne::Chain does not modify content itself, it allows any of the modules below to intercept the request as if they had been loaded by the target page directly (i.e.,
      loaded in the __PERL__ section of a page via the "use" or "require" functions).</para>

      <para>Using WebDyne::Chain you can modify the behaviour of WebDyne pages based on their location. The WebDyne::Template module can be used in such scenario to wrap all pages in a
      location with a particular template. Another would be to make all pages in a particular location static without loading the WebDyne::Static module in each page:<programlisting>&lt;Location /static&gt;

#  All pages in this location will be generated once only.
PerlHandler     WebDyne::Chain
PerlSetVar      WebDyneChain    'WebDyne::Static'

&lt;/Location&gt;</programlisting></para>

      <para>Multiple modules can be chained at once:</para>

      <programlisting>&lt;Location /&gt;

#  We want templating and session cookies for all pages on our site.
PerlHandler     WebDyne::Chain
PerlSetVar      WebDyneChain    'WebDyne::Session WebDyne::Template'
PerlSetVar      WebDyneTemplate '/path/to/template.psp'

&lt;/Location&gt;</programlisting>

      <para>The above example would place all pages within the named template, and make session information to all pages via <code>$self-&gt;session_id()</code>. A good start to a rudimentary
      CMS.</para>

      <variablelist>
        <varlistentry>
          <term><methodname>WebDyneChain</methodname></term>

          <listitem>
            <para>Directive. Supply a space separated string of WebDyne modules that the request should be passed through.</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </section>

    <section>
      <info>
        <title>WebDyne::Static</title>
      </info>

      <para>Loading WebDyne::Static into a __PERL__ block flags to WebDyne that the entire page should be rendered once at compile time, then the static HTML resulting from that compile will
      be handed out on subsequent requests. Any active element or code in the page will only be run once. There are no API methods associated with this module</para>

      <para>See the <link linkend="static_sections">Static Sections</link> reference for more information on how to use this module within an individual page.</para>

      <para>WebDyne::Static can also be used in conjunction with the <link linkend="webdyne_chain">WebDyne::Chain</link> module to flag all files in a directory or location as static. An
      example httpd.conf snippet:</para>

      <para><programlisting>&lt;Location /static/&gt;

PerlHandler     WebDyne::Chain
PerlSetVar      WebDyneChain    'WebDyne::Static'

&lt;/Location&gt;
</programlisting></para>
    </section>

    <section>
      <info>
        <title>WebDyne::Cache</title>
      </info>

      <para>Loading WebDyne::Cache into a __PERL__ block flags to WebDyne that the page wants the engine to call a designated routine every time it is run. The called routine can generate a
      new UID (Unique ID) for the page, or force it to be recompiled. There are no API methods associated with this module.</para>

      <para>See the <link linkend="caching_section">Caching</link> section above for more information on how to use this module with an individual page.</para>

      <para>WebDyne::Cache can also be used in conjunction with the <link linkend="webdyne_chain">WebDyne::Chain</link> module to flag all files in a particular location are subject to a cache
      handling routine. An example httpd.conf snippet:</para>

      <para><programlisting>&lt;Location /cache/&gt;

#  Run all requests through the MyModule::cache function to see if a page should
#  be recompiled before sending it out
#
PerlHandler     WebDyne::Chain
PerlSetVar      WebDyneChain    'WebDyne::Cache'
PerlSetVar      WebDyneCacheHandler '&amp;MyModule::cache'

&lt;/Location&gt;
</programlisting></para>

      <para>Note that any package used as the <varname>WebDyneCacheHandler</varname> target should be already loaded via "PerlRequire" or similar mechanism.</para>

      <para>As an example of why this could be useful consider the <link linkend="caching_section">caching examples</link> above. Instead of flagging that an individual file should only be
      re-compiled every "x" seconds, that policy could be applied to a whole directory with no alteration to the individual pages.</para>
    </section>

    <section>
      <info>
        <title>WebDyne::Session</title>
      </info>

      <para>WebDyne::Session generates a unique session ID for each browser connection and stores it in a cookie. It has the following API:</para>

      <variablelist>
        <varlistentry>
          <term><methodname>session_id()</methodname></term>

          <listitem>
            <para>Function. Returns the unique session id assigned to the browser. Call via <code>$self-&gt;session_id()</code> from perl code.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><varname>$WEBDYNE_SESSION_ID_COOKIE_NAME </varname></term>

          <listitem>
            <para>Constant. Holds the name of the cookie that will be used to assign the session id in the users browser. Defaults to "session". Set as per <link
            linkend="webdyne_constants">WebDyne::Constants</link> section. Resides in the <classname>WebDyne::Session::Constant</classname> package namespace.</para>
          </listitem>
        </varlistentry>
      </variablelist>

      <para>Example:</para>

      <example>
        <info>
          <title>WebDyne Session module</title>
        </info>

        <para><programlisting><xi:include href="example/session1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting></para>

        <para><link xlink:href="https://demo.webdyne.org/example/session1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>WebDyne::Session can also be used in conjunction with the <link linkend="webdyne_chain">WebDyne::Chain</link> module to make session information available to all pages within a
      location. An example httpd.conf snippet:</para>

      <para><programlisting>&lt;Location /&gt;

# We want session cookies for our whole site
#
PerlHandler     WebDyne::Chain
PerlSetVar      WebDyneChain    'WebDyne::Session'

#  Change cookie name from "session" to "gingernut" for something different
#
PerlSetVar      WEBDYNE_SESSION_ID_COOKIE_NAME    'gingernut'

&lt;/Location&gt;
</programlisting></para>
    </section>

    <section>
      <info>
        <title>WebDyne::Template</title>
      </info>

      <para>One of the more powerful WebDyne extensions. WebDyne::Template can be used to build CMS (Content Management Systems). It will extract the &lt;head&gt; and &lt;body&gt; sections
      from an existing HTML or WebDyne page and insert them into the corresponding head and body blocks of a template file.</para>

      <para>The merging is done once at compile time - there are no repeated search and replace operations each time the file is loaded, or server side includes, so the resulting pages are
      quite fast.</para>

      <para>Both the template and content files should be complete - there is no need to write the content without a <tag>&lt;head&gt;</tag> section, or leave out <tag>&lt;html&gt;</tag> tags.
      As a result both the content and template files can be viewed as standalone documents.</para>

      <para>The API:</para>

      <variablelist>
        <varlistentry>
          <term><methodname>template ( filename )</methodname></term>

          <listitem>
            <para>Function. Set the file name of the template to be used. If no path is specified file name will be relative to the current request directory</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>WebDyneTemplate</term>

          <listitem>
            <para>Directive. Can be used to supply the template file name in a Apache Dir_Config section, or a the WEBDYNE_DIR_CONFIG section of a webdyne.conf.pl file for PSGI</para>
          </listitem>
        </varlistentry>
      </variablelist>

      <para>Example:</para>

      <para>The template:</para>

      <example>
        <info>
          <title>Template example</title>
        </info>

        <para><programlisting language="html"><xi:include href="example/template1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting></para>

        <para><link xlink:href="https://demo.webdyne.org/example/template1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>The content, run to view resulting merge:</para>

      <example>
        <info>
          <title>Template content</title>
        </info>

        <para><programlisting><xi:include href="example/template_home1.psp" parse="text" xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting></para>

        <para><link xlink:href="https://demo.webdyne.org/example/template_home1.psp" xmlns:xlink="http://www.w3.org/1999/xlink">Run</link></para>
      </example>

      <para>In real life it is not desirable to put the template name into every content file (as was done in the above example), nor would we want to have to "use WebDyne::Template" in every
      content file.</para>

      <para>To overcome this WebDyne::Template can read the template file name using the Apache <function>dir_config</function> function, and assign a template on a per location basis using
      the WebDyneTemplate directive. Here is a sample <filename>httpd.conf</filename> file:</para>

      <para><programlisting>&lt;Location /&gt;

PerlHandler     WebDyne::Chain
PerlSetVar      WebDyneChain    'WebDyne::Template'
PerlSetVar      WebDyneTemplate '/path/to/template.psp'

&lt;/Location&gt;

</programlisting></para>

      <para>Alternatively in a PSGI environment you can create a .webdyne.conf.pl file in the directory with the template directives as follows:</para>

      <programlisting>$_={
    'WebDyne::Constant' =&gt; {
        WEBDYNE_DIR_CONFIG =&gt; {
            '' =&gt; {
                'WebDyneHandler'    =&gt; 'WebDyne::Chain',
                'WebDyneChain'      =&gt; 'WebDyne::Template',
                'WebDyneTemplate'   =&gt; 'template.psp'
            },
        },
    }
}</programlisting>
    </section>
  </section>

  <section xml:id="troubleshooting">
    <title>Troubleshooting</title>

    <para>At some stage a PSP file is not going to output what you expect. This could be for many reasons:</para>

    <itemizedlist>
      <listitem>
        <para>Incorrectly closed or matched HTML tags in the source</para>
      </listitem>

      <listitem>
        <para>Mismatched quotation symbols for attributes</para>
      </listitem>

      <listitem>
        <para>Incorrect syntax in WebDyne tags, code or attributes</para>
      </listitem>

      <listitem>
        <para>Failure by the HTML Parser to read complex or unusual HTML</para>
      </listitem>

      <listitem>
        <para>A bug in the WebDyne code at runtime</para>
      </listitem>
    </itemizedlist>

    <para>There are several troubleshooting steps:</para>

    <orderedlist>
      <listitem>
        <para>Does the file render from the command line using <command>wdrender</command> ? Are there any additional warnings or error messages generated from the command line ?</para>
      </listitem>

      <listitem>
        <para>Is the file validly constructed and balanced HTML when read by the WebDyne parser ? Use <command>wdcompile</command> to check the parsed HTML and check for any errors in
        attributes or tags</para>
      </listitem>

      <listitem>
        <para>Use the <command>wddebug --enable</command> and <envar>WEBDYNE_DEBUG=1</envar> environment variable to elicit more information</para>
      </listitem>
    </orderedlist>

    <para>If nothing obvious jumps out put a bug report on the <link xlink:href="https://github.com/aspeer/WebDyne" xmlns:xlink="http://www.w3.org/1999/xlink">WebDyne Github</link> page for
    the author to review.</para>
  </section>

  <section xml:id="credits">
    <info>
      <title>Credits</title>
    </info>

    <para>WebDyne relies heavily on modules and code developed and open-sourced by other authors. Without Perl, and Perl modules such as <classname>mod_perl/PSGI</classname>,
    <classname>HTML::Tiny</classname>, <classname>HTML::TreeBuilder</classname>, <classname>Storable</classname> and many others, WebDyne would not be possible. To the authors of those modules
    - and all the other modules used to a lesser extent by WebDyne - I convey my thanks.</para>
  </section>

  <section xml:id="additional_notes">
    <info>
      <title>Additional Notes</title>
    </info>

    <para>Things to note or information not otherwise contained elsewhere:</para>

    <section xml:id="additional_notes_handler_attribute_shortcuts">
      <title>About handler and method calling attributes in tags</title>

      <para>Some WebDyne attributes name a Perl subroutine to call. In PSP syntax an attribute with no explicit value is treated as having the attribute name as its value, so
      <tag>&lt;perl handler&gt;</tag> is a shortcut for <tag>&lt;perl handler="handler"&gt;</tag>. Defining a <function>handler</function> subroutine in the <token>__PERL__</token> section is
      therefore the shortest way to make a simple PSP file call server-side Perl code:</para>

      <programlisting language="html">&lt;start_html&gt;
&lt;perl handler/&gt;

__PERL__

sub handler {
    my $self=shift();
    return "Hello from a handler";
}</programlisting>

      <para>For clarity, or where a page has more than one callable routine, the longer form can be used instead. For example <tag>&lt;perl handler="my_handler"&gt;</tag> calls
      <function>my_handler</function> and behaves the same way; it just takes more typing:</para>

      <programlisting language="html">&lt;start_html&gt;
&lt;perl handler="my_handler"/&gt;

__PERL__

sub my_handler {
    my $self=shift();
    return "Hello from my_handler";
}</programlisting>

      <para>The same bare-attribute convention applies to other handler-style attributes in the context where those attributes are valid. In <tag>&lt;perl&gt;</tag>, <tag>&lt;api&gt;</tag> and
      <tag>&lt;htmx&gt;</tag>, <property>handler</property> names the Perl routine to call. In <tag>&lt;start_html&gt;</tag>, <property>sse</property> and <property>ws</property> name the PAGI
      Server-Sent Events and WebSocket handlers, so <tag>&lt;start_html sse&gt;</tag> means <tag>&lt;start_html sse="sse"&gt;</tag>, and <tag>&lt;start_html ws&gt;</tag> means
      <tag>&lt;start_html ws="ws"&gt;</tag>. The <property>cache</property> attribute on <tag>&lt;start_html&gt;</tag> similarly names the cache handler used by the caching layer. The
      <property>method</property> attribute is accepted as a compatibility synonym for <property>handler</property> on <tag>&lt;perl&gt;</tag>-style calls, but
      <property>handler</property> is preferred in new examples.</para>
    </section>

    <section xml:id="additional_notes_start_html_shortcuts">
      <title>About <tag>&lt;start_html&gt;</tag> shortcuts</title>

      <para>The <tag>&lt;start_html&gt;</tag> tag exists as a convenience for common page setup. It expands to normal HTML, including the document type, opening <tag>&lt;html&gt;</tag> and
      <tag>&lt;body&gt;</tag> tags, and a generated <tag>&lt;head&gt;</tag> section. It is not a separate client-side technology; the browser receives ordinary HTML output.</para>

      <para>Some <tag>&lt;start_html&gt;</tag> attributes are small formatting shortcuts intended for quick page construction. For example <property>h1</property> through
      <property>h6</property> insert a heading based on the page title, and <property>hr</property> adds a horizontal rule after that generated heading. These are useful for quick examples,
      diagnostics and small tools, but they can always be replaced with explicit HTML in the body of the page.</para>

      <para>Other <tag>&lt;start_html&gt;</tag> shortcuts load common frontend libraries or stylesheets. The built-in shortcuts include <property>pico</property> for Pico CSS,
      <property>htmx</property> for htmx, and <property>alpine</property> for Alpine.js. They exist to keep small PSP examples readable and to avoid repeating common CDN
      <tag>&lt;link&gt;</tag> or <tag>&lt;script&gt;</tag> tags. For example <tag>&lt;start_html alpine&gt;</tag> loads the configured Alpine.js script. Longer form HTML is still supported, so
      you can load JavaScript or CSS in the traditional way with explicit <tag>&lt;script&gt;</tag> and <tag>&lt;link&gt;</tag> tags, or by using the <property>script</property> and
      <property>style</property> attributes documented in the <tag>&lt;start_html&gt;</tag> tag reference.</para>
    </section>

    <section xml:id="additional_notes_check_psp_syntax">
      <title>How to check syntax of a PSP file</title>

      <para>To check the syntax of a PSP file - or more specifically any Perl code in the __PERL__ section - you can use the <command>wdlint</command> command. Take this file with an
      assignment syntax error in the server_time() routine:</para>

      <programlisting language="html">&lt;start_html&gt;
Hello World &lt;? server_time() ?&gt;
__PERL__
#!perl

sub server_time {
    my 2==1; #Error here
}</programlisting>

      <para>Run the command<command> wdlint &lt;filename.psp&gt;</command> to check for syntax error and report back:</para>

      <screen>$ wdlint check.psp
syntax error at check.psp line 8, near "my 2"
check.psp had compilation errors.</screen>
    </section>

    <section xml:id="additional_notes_processing_instruction_self">
      <title>How to pass <varname>$self</varname> ref if using processing instructions</title>

      <para>If you use the processing instruction form of calling a perl method it will not pass the WebDyne object ref through to your code. You can pass it by supplying @_ as a parameter, or
      just shift() and your own parameters:</para>

      <programlisting language="html">&lt;start_html&gt;
Hello World &lt;? server_time(@_) ?&gt;
Hello World &lt;? server_time(shift(), 'UTC' ?&gt;
Hello World &lt;perl handler="server_time" param="UTC"/&gt;
__PERL__

sub server_time {
    #  Now we can get self ref
    my ($self, $timezone)=@_;

    #  Do something and return
    $self-&gt;do_something()
}</programlisting>
    </section>

    <section xml:id="additional_notes_hash_comment_characters">
      <title>Use of hash characters for comments in PSP files</title>

      <para>Any # characters at the very start of a PSP file (before a <tag>&lt;html&gt;</tag> or <tag>&lt;start_html&gt;</tag>) tag are treated as comments and discarded - they will not be
      stored or displayed (they are <emphasis role="bold">not</emphasis> translated into HTML comments). This allows easy to read comments at the start of PSP files. Any # characters after the
      first valid tag are not treated specially - they will be rendered as normal HTML:</para>

      <programlisting language="html">#  This is my server time display file
#
#  VERSION=1.23
#
&lt;start_html&gt;
Server local time is: &lt;? localtime ?&gt;</programlisting>
    </section>

    <section xml:id="additional_notes_checkbox_hidden_field">
      <title>The <tag>&lt;checkbox&gt;</tag> tag will always set a hidden form field</title>

      <para>The <tag>&lt;checkbox&gt;</tag> tag is unusual in that it adds a hidden field (with the same name as the checkbox) to the HTML page to retain state. Thus if you are examining the
      checkbox parameter from CGI via <varname>$_{'checkbox_name'}</varname> or <code>$self-&gt;CGI-&gt;param('checkbox_name')</code> you may get an array rather than a single value. The value
      of the checkbox (boolean, checked or unchecked, i.e. 1 or 0) will always be the first value returned. So the code <code>if ($_{'checkbox_name'}) { .. do_something }</code> will work as
      expected - but just be careful if using in an array context.</para>
    </section>

    <section xml:id="additional_notes_about_documentation">
      <title>About this documentation</title>

      <para>This documentation is written with the <link xlink:href="https://www.xmlmind.com/xmleditor/" xmlns:xlink="http://www.w3.org/1999/xlink">XMLMind XML Editor</link>, then converted to
      Markdown with <link xlink:href="https://pandoc.org" xmlns:xlink="http://www.w3.org/1999/xlink">pandoc</link> and displayed using <link xlink:href="https://www.mkdocs.org"
      xmlns:xlink="http://www.w3.org/1999/xlink">MKdocs</link>. The documentation for WebDyne is maintained on a <link xlink:href="https://github.com/aspeer/WebDyne"
      xmlns:xlink="http://www.w3.org/1999/xlink">Github repository</link>.</para>
    </section>
  </section>

  <section xml:id="legal_information">
    <info>
      <title>Legal Information - Licensing and Copyright</title>
    </info>

    <para>WebDyne is Copyright © Andrew Speer 2006-2025. WebDyne is free software; you can redistribute it and/or modify it under the same terms as Perl itself.</para>

    <para>WebDyne is written in Perl and uses modules from <link xlink:href="http://www.cpan.org" xmlns:xlink="http://www.w3.org/1999/xlink">CPAN</link> (the Comprehensive Perl Archive
    Network). CPAN modules are Copyright © the owner/author, and are available in source form by downloading from CPAN directly. All CPAN modules used are covered by the <link
    xlink:href="http://www.perl.com/pub/a/language/misc/Artistic.html" xmlns:xlink="http://www.w3.org/1999/xlink">Perl Artistic License</link></para>
  </section>
</article>
